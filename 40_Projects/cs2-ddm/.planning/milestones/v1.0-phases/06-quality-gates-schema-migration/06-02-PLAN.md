---
phase: 06-quality-gates-schema-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - duel_attempts.py
  - kill_rate_analysis.py
autonomous: true
requirements:
  - "SC3: Both output schemas include player_steamid column"
  - "SC4: All 215 existing tests remain green"

must_haves:
  truths:
    - "DuelAttempt dataclass имеет поле player_steamid: Optional[int] = None"
    - "DuelAttemptFinder._process_cluster() записывает self.player_steamid в DuelAttempt"
    - "save_attempts() выполняет append+dedup по match_id (не перезаписывает весь файл)"
    - "Повторный запуск save_attempts() с тем же match_id не дублирует строки в CSV"
  artifacts:
    - path: "duel_attempts.py"
      provides: "DuelAttempt.player_steamid field"
      contains: "player_steamid: Optional[int] = None"
    - path: "kill_rate_analysis.py"
      provides: "save_attempts() с dedup по match_id"
      contains: "match_id"
  key_links:
    - from: "kill_rate_analysis.run_player()"
      to: "save_attempts()"
      via: "match_id передаётся из run_player loop"
      pattern: "save_attempts.*match_id"
---

<objective>
Добавить `player_steamid` в Path 2 схему (DuelAttempt dataclass) и исправить `save_attempts()` чтобы она делала append+dedup по match_id вместо перезаписи всего файла.

Purpose: D-05 требует player_steamid в обоих output путях. D-06 требует идемпотентность для DuelAttemptFinder — без неё повторный запуск дублирует строки в `*_attempts.csv`.

Output: `duel_attempts.py` с player_steamid полем, `kill_rate_analysis.py` с dedup логикой в save_attempts().
</objective>

<execution_context>
@D:/Obsidian/opacity/40_Projects/cs2-ddm/.claude/workflows/execute-plan.md
</execution_context>

<context>
@D:/Obsidian/opacity/40_Projects/cs2-ddm/.planning/phases/06-quality-gates-schema-migration/06-CONTEXT.md
@D:/Obsidian/opacity/40_Projects/cs2-ddm/.planning/phases/06-quality-gates-schema-migration/06-PATTERNS.md

<interfaces>
<!-- Extracted from duel_attempts.py -->

DuelAttempt dataclass (lines 27-50):
```python
@dataclass
class DuelAttempt:
    match_id: str
    map_name: str
    t0_tick: int
    enemy_steamid: int
    was_killed: bool
    bullets_fired: int
    bullets_hit: int
    engagement_type: str
    player_velocity_ups: float
    crosshair_angle_deg: float
    hurt_victims_in_window: str = ""
    fires_in_cluster: int = 0
```

DuelAttemptFinder.__init__ (lines 68-80):
```python
def __init__(
    self,
    player_steamid: int,
    match_id: str,
    ...
):
    self.player_steamid = player_steamid
    self.match_id = match_id
```

_process_cluster return (line 199):
```python
return DuelAttempt(
    match_id=self.match_id,
    map_name=self.map_name,
    ...
)
```

<!-- Extracted from kill_rate_analysis.py -->

save_attempts current (lines 83-89) — НЕТ dedup:
```python
def save_attempts(name: str, attempts: List[DuelAttempt]) -> None:
    if not attempts:
        return
    rows = [dataclasses.asdict(a) for a in attempts]
    path = f"{name}_attempts.csv"
    pd.DataFrame(rows).to_csv(path, index=False)
    print(f"  Saved {len(rows)} rows → {path}")
```

run_player() call site (line 152):
```python
save_attempts(name, attempts)
```

<!-- csv_utils.save_results() dedup pattern (аналог для D-06) -->
Pattern из csv_utils.py lines 38-53:
```python
existing = load_existing_results(filename)
if not existing.empty and "match_id" in existing.columns:
    existing = existing[existing["match_id"].astype(str) != str(match_id)]
    combined = pd.concat([existing, results_df], ignore_index=True)
else:
    combined = results_df
combined.to_csv(filename, index=False)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: player_steamid в DuelAttempt dataclass (D-05)</name>
  <files>duel_attempts.py, tests/test_ddm_analyzer_quality.py</files>
  <read_first>
    - duel_attempts.py (полный файл — нужен DuelAttempt dataclass и _process_cluster return)
    - duel_attempts.py lines 68–80 (verify DuelAttemptFinder.__init__ already has player_steamid param)
  </read_first>
  <behavior>
    - Test 1: DuelAttempt можно создать без player_steamid (дефолт None)
    - Test 2: DuelAttempt с явным player_steamid=76561198386265483 сохраняет значение
    - Test 3: dataclasses.asdict(attempt) содержит ключ "player_steamid"
    - Test 4: DuelAttemptFinder с player_steamid=X генерирует DuelAttempt.player_steamid=X (интеграционный)
  </behavior>
  <action>
**Шаг 1 — написать тесты RED:**

В `tests/test_ddm_analyzer_quality.py` или отдельном файле добавить `TestDuelAttemptPlayerSteamid`.

**Шаг 2 — изменить `duel_attempts.py`:**

1. Убедиться что `from typing import Optional` импортирован (уже должен быть).

2. В `DuelAttempt` dataclass добавить поле в конец (после `fires_in_cluster`):
```python
player_steamid: Optional[int] = None  # D-05: player identity for cross-player queries
```

3. В методе `_process_cluster()` найти return DuelAttempt(...) и добавить:
```python
player_steamid=self.player_steamid,
```
  </action>
  <verify>
    <automated>cd "D:/Obsidian/opacity/40_Projects/cs2-ddm" && python -m pytest tests/ -k "DuelAttemptPlayerSteamid" -v -p no:cov 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "player_steamid" duel_attempts.py` выводит >= 3 (в dataclass, в _process_cluster return, возможно в __init__)
    - `python -c "from duel_attempts import DuelAttempt; a = DuelAttempt('m1','de_dust2',100,123,True,5,3,'peek',100.0,5.0); print(a.player_steamid)"` выводит `None` (дефолт работает)
    - `python -c "import dataclasses; from duel_attempts import DuelAttempt; a = DuelAttempt('m1','de_dust2',100,123,True,5,3,'peek',100.0,5.0,player_steamid=76561198386265483); print(dataclasses.asdict(a)['player_steamid'])"` выводит `76561198386265483`
  </acceptance_criteria>
  <done>DuelAttempt.player_steamid поле существует с дефолтом None; _process_cluster записывает self.player_steamid</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: save_attempts() append+dedup по match_id (D-06)</name>
  <files>kill_rate_analysis.py, tests/test_csv_utils.py</files>
  <read_first>
    - kill_rate_analysis.py lines 61-160 (run_player, save_attempts, load_attempts, main)
    - csv_utils.py lines 1-60 (dedup pattern аналог)
    - tests/test_csv_utils.py (паттерны существующих тестов для csv dedup)
  </read_first>
  <behavior>
    - Test 1: save_attempts() при отсутствующем CSV создаёт новый файл с N строками
    - Test 2: повторный вызов save_attempts() с тем же match_id — в итоге N строк (не 2N)
    - Test 3: вызов save_attempts() с другим match_id — в итоге N1+N2 строк (аккумуляция)
    - Test 4: save_attempts() с пустым списком — файл не создаётся / не изменяется
    - Test 5: run_player() передаёт match_id явно в save_attempts() — использовать unittest.mock.patch("kill_rate_analysis.save_attempts") и проверить call_args: первый positional == name, второй positional == attempts, keyword match_id == ожидаемый match_id из demo
    - Test 6: два разных match_id в одном батче (multi-demo) сохраняются независимо — создать два DuelAttempt с match_id="demo_a" и match_id="demo_b", вызвать save_attempts дважды для каждого match_id, итог == 2 строки (по одной на match_id), не 4
  </behavior>
  <action>
**Шаг 1 — написать тесты RED** для `TestSaveAttempts` в `tests/test_csv_utils.py` или в новом `tests/test_kill_rate_analysis.py`.

Обязательно включить Test 5 (mock-проверка передачи match_id из run_player) и Test 6 (multi-demo батч).

**Шаг 2 — изменить `kill_rate_analysis.py`:**

Добавить импорты на верхний уровень файла (не внутри функций):
```python
import itertools
import os
```

Заменить `save_attempts()` (lines 83-89) на:
```python
def save_attempts(name: str, attempts: List[DuelAttempt], match_id: str) -> None:
    """Append+dedup by match_id — аналог csv_utils.save_results() (D-06).

    match_id обязателен — без него невозможно гарантировать корректный dedup.
    """
    if not attempts:
        return
    rows = pd.DataFrame([dataclasses.asdict(a) for a in attempts])
    path = f"{name}_attempts.csv"
    if os.path.exists(path) and os.path.getsize(path) > 0:
        try:
            existing = pd.read_csv(path, dtype=str)
            if "match_id" in existing.columns:
                existing = existing[existing["match_id"].astype(str) != str(match_id)]
            combined = pd.concat([existing, rows.astype(str)], ignore_index=True)
        except Exception:
            combined = rows
    else:
        combined = rows
    combined.to_csv(path, index=False)
    print(f"  -> Saved {len(rows)} rows for match_id={match_id} (CSV total: {len(combined)} rows).")
```

**Шаг 3 — обновить вызов в `run_player()` и `main()`:**

В `run_player()` найти `save_attempts(name, attempts)` и заменить на явную передачу match_id:

```python
# В run_player() — match_id берётся из параметра функции или из DuelAttempt объектов
# Если run_player принимает match_id напрямую:
save_attempts(name, attempts, match_id=match_id)
```

В `main()` — использовать itertools.groupby для per-match_id dedup:

```python
# В main() заменить save_attempts(name, attempts) на:
if attempts:
    for mid, group in itertools.groupby(
        sorted(attempts, key=lambda a: a.match_id), key=lambda a: a.match_id
    ):
        save_attempts(name, list(group), match_id=mid)
```

Это гарантирует что каждый demo_match_id dedup'ится независимо — точная семантика csv_utils.save_results().
  </action>
  <verify>
    <automated>cd "D:/Obsidian/opacity/40_Projects/cs2-ddm" && python -m pytest tests/ -k "SaveAttempts or save_attempts" -v -p no:cov 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "match_id" kill_rate_analysis.py` выводит >= 3 (в save_attempts signature, в dedup logic, в main call)
    - `grep "def save_attempts" kill_rate_analysis.py` выводит строку с `match_id: str` без дефолта "default"
    - `grep "^import itertools\|^import os" kill_rate_analysis.py` — оба импорта на верхнем уровне
    - Тесты idempotency проходят: повторный вызов с тем же match_id → в CSV N строк, не 2N
    - Test 5 проходит: mock подтверждает что run_player() передаёт match_id явно в save_attempts()
    - Multi-demo тест (Test 6) проходит: два разных match_id → 2 строки (не 4) после двух пар вызовов
    - `python -m pytest -p no:cov 2>&1 | tail -3` показывает все тесты зелёными
  </acceptance_criteria>
  <done>save_attempts() идемпотентна: повторный запуск с тем же match_id не дублирует строки; match_id обязателен (нет дефолта — data-loss невозможен); импорты os и itertools на верхнем уровне; run_player() передаёт match_id явно</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-|-|
| existing CSV → read | Файл может быть повреждён или иметь неправильный формат |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-|-|-|-|-|
| T-06-03 | Tampering | save_attempts CSV read | mitigate | try/except вокруг pd.read_csv — fallback на перезапись если файл повреждён |
| T-06-04 | Information Disclosure | player_steamid в CSV | accept | SteamID64 это публичный идентификатор, не секрет |
</threat_model>

<verification>
```bash
cd "D:/Obsidian/opacity/40_Projects/cs2-ddm"
python -m pytest -p no:cov 2>&1 | tail -5
python -c "from duel_attempts import DuelAttempt; import dataclasses; a = DuelAttempt('m','de_dust2',1,2,True,1,1,'peek',50.0,5.0); print('player_steamid' in dataclasses.asdict(a))"
grep "def save_attempts" kill_rate_analysis.py
grep "^import itertools\|^import os" kill_rate_analysis.py
```
</verification>

<success_criteria>
- DuelAttempt.player_steamid поле существует с дефолтом None
- _process_cluster() записывает player_steamid из self.player_steamid
- save_attempts() принимает match_id без дефолта (обязательный параметр), делает dedup, не дублирует строки при повторном запуске
- run_player() передаёт match_id явно — проверено mock-тестом
- import os и import itertools на верхнем уровне kill_rate_analysis.py
- Multi-demo батч: каждый match_id dedup'ится независимо через itertools.groupby в main()
- Все 215+ тестов зелёные
</success_criteria>

<output>
После завершения создать `.planning/phases/06-quality-gates-schema-migration/06-03-SUMMARY.md`
</output>
