---
phase: 06-quality-gates-schema-migration
plan: 04
type: execute
wave: 3
depends_on:
  - "06-01-PLAN"
  - "06-02-PLAN"
  - "06-03-PLAN"
files_modified:
  - tests/test_ddm_analyzer_quality.py
  - tests/test_db_utils.py
  - tests/test_t0_detector_find_t0.py
autonomous: true
requirements:
  - "SC1: RT > 3000ms never produced due to T0-at-search-start (regression guard)"
  - "SC2: Duplicate moment guard verified by test"
  - "SC3: player_steamid column present in both schemas — SQL query test"
  - "SC4: All 215 existing tests remain green after migration"

must_haves:
  truths:
    - "Все 215 изначальных тестов остаются зелёными после всех изменений планов 01-03"
    - "Финальный pytest count >= 215 (новые тесты добавлены, ни один не удалён)"
    - "Phase 6 success criteria 1-4 верифицированы автоматическими тестами"
    - "Тест для T0_MIN_OFFSET_TICKS edge case существует и проходит: find_t0() с t0=search_start_tick возвращает None"
  artifacts:
    - path: "tests/test_ddm_analyzer_quality.py"
      provides: "Tests for quality gates (overlapping window + teammate)"
      contains: "TestOverlappingWindowGate"
    - path: "tests/test_db_utils.py"
      provides: "Tests for SQLite dual-write"
      contains: "test_save_to_db"
    - path: "tests/test_t0_detector_find_t0.py"
      provides: "Regression test for T0_MIN_OFFSET_TICKS edge case (SC1)"
      contains: "t0_min_offset"
  key_links:
    - from: "pytest suite"
      to: "ddm_analyzer.py quality gates"
      via: "test_ddm_analyzer_quality.py"
      pattern: "TestOverlappingWindowGate|TestTeammateGate"
    - from: "pytest suite"
      to: "db_utils.py"
      via: "test_db_utils.py"
      pattern: "test_save_to_db|test_idempotency"
    - from: "pytest suite"
      to: "T0_MIN_OFFSET_TICKS in t0_detector.py"
      via: "test_t0_detector_find_t0.py"
      pattern: "t0_min_offset"
---

<objective>
Финальная верификация Phase 6: запустить полный test suite, проверить все 4 success criteria, убедиться что тест для T0_MIN_OFFSET_TICKS edge case существует, зафиксировать итоговое состояние.

Purpose: Убедиться что все изменения планов 01-03 не сломали ни один из 215 существующих тестов, и что новые тесты покрывают все Phase 6 success criteria. check-phase6 skill требует явный тест для Edge Case 3.

Output: Зелёный test suite с финальным count >= 215, плюс чеклист Phase 6 SC.
</objective>

<execution_context>
@D:/Obsidian/opacity/40_Projects/cs2-ddm/.claude/workflows/execute-plan.md
</execution_context>

<context>
@D:/Obsidian/opacity/40_Projects/cs2-ddm/.planning/phases/06-quality-gates-schema-migration/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Полный regression run + gap-fill недостающих тестов</name>
  <files>tests/test_ddm_analyzer_quality.py, tests/test_db_utils.py, tests/test_t0_detector_find_t0.py</files>
  <read_first>
    - tests/test_ddm_analyzer_quality.py (проверить что классы TestOverlappingWindowGate и TestTeammateGate добавлены планами 01-03)
    - tests/test_db_utils.py (проверить что idempotency тесты добавлены планом 03)
    - tests/test_t0_detector_find_t0.py (проверить наличие теста для T0_MIN_OFFSET_TICKS edge case)
    - Запустить `python -m pytest --collect-only -q` и посмотреть count
  </read_first>
  <action>
**Шаг 1 — запустить полный suite:**
```bash
cd "D:/Obsidian/opacity/40_Projects/cs2-ddm"
python -m pytest -p no:cov -v 2>&1 | tail -30
```

**Шаг 2 — проверить существование теста T0_MIN_OFFSET_TICKS (SC1):**

```bash
grep -n "t0_min_offset\|T0_MIN_OFFSET\|search_start.*None\|t0.*search_start" \
    tests/test_t0_detector_find_t0.py tests/test_ddm_analyzer_quality.py 2>/dev/null
```

Если тест НЕ найден ни в одном файле — прочитать реальную сигнатуру `find_t0()` в `t0_detector.py`, затем создать его в `tests/test_t0_detector_find_t0.py`:

```python
from config import T0_MIN_OFFSET_TICKS

@pytest.mark.parametrize("offset,expected", [
    (0, None),   # t0 = search_start → None (< T0_MIN_OFFSET_TICKS=20)
    (19, None),  # t0 = search_start + 19 → None (ещё < 20)
    (20, "not_none"),  # t0 = search_start + 20 → принимается (== T0_MIN_OFFSET_TICKS)
    (21, "not_none"),  # t0 = search_start + 21 → принимается
])
def test_t0_min_offset_ticks_gate(offset, expected, <fixtures_matching_find_t0_signature>):
    """SC1 / Edge Case 3: T0_MIN_OFFSET_TICKS gate — find_t0() отвергает t0 < search_start + 20.

    Если BVH находит T0 = search_start_tick, это аномалия (visibility с начала окна → реальный
    T0 ещё раньше). Такие моменты отвергаются через T0_MIN_OFFSET_TICKS=20.
    """
    # Настроить мок VisibilityChecker чтобы is_visible возвращал True
    # только начиная с search_start_tick + offset (чтобы симулировать нужный t0)
    # Конкретная настройка зависит от сигнатуры find_t0 — адаптировать после прочтения файла

    # t0_tick = search_start_tick + offset
    # Вызвать find_t0(...)
    result = find_t0(...)  # адаптировать под реальную сигнатуру

    if expected is None:
        assert result is None, (
            f"offset={offset}: expected None (< T0_MIN_OFFSET_TICKS={T0_MIN_OFFSET_TICKS}), got {result}"
        )
    else:
        assert result is not None, (
            f"offset={offset}: expected valid T0 (>= T0_MIN_OFFSET_TICKS={T0_MIN_OFFSET_TICKS}), got None"
        )
```

ВАЖНО: адаптировать под реальную сигнатуру `find_t0()` после прочтения `t0_detector.py`. Параметры выше — шаблон.

**Шаг 3 — если есть failures после планов 01-03, диагностировать и исправить:**

Типичные причины поломки:
a) Фикстуры DDMAnalyzer создают объект без `player_steamid` → добавить `player_steamid=76561198386265483` в вызовах фикстур.
b) Сигнатура `save_attempts()` изменена (match_id теперь обязателен) — существующие вызовы без match_id упадут с TypeError → найти все call-sites и добавить явный match_id.
c) TypeError из-за новых обязательных параметров → сделать параметры опциональными с разумными дефолтами там, где это безопасно.

**Шаг 4 — проверить Phase 6 Success Criteria:**

SC1 (T0 inflation): `python -m pytest -k "t0_min_offset" -p no:cov` — тест проходит.

SC2 (overlapping window): `python -m pytest -k "Overlapping" -v -p no:cov` — >= 5 тестов, все зелёные.

SC3 (player_steamid в SQL): `python -m pytest tests/test_db_utils.py -k "player_steamid" -v -p no:cov` — тест проверяет что после save_to_db() запрос `SELECT player_steamid FROM engagements WHERE player_steamid=X` возвращает строки.

SC4 (все тесты зелёные): финальный `python -m pytest -p no:cov 2>&1 | tail -3` выводит count >= 215 и 0 failures.

**Если SC3 тест отсутствует** — добавить в `tests/test_db_utils.py`:
```python
def test_save_to_db_player_steamid_queryable(tmp_path):
    """SC3: SQL query by player_steamid returns rows."""
    import sqlite3
    import pandas as pd
    from db_utils import save_to_db
    db = str(tmp_path / "test.db")
    df = pd.DataFrame({
        "match_id": ["match1"],
        "player_steamid": [76561198386265483],
        "map_name": ["de_ancient"],
        "rt_visible_to_hit_ms": [250.0],
    })
    save_to_db(df, db, "engagements", "match1")
    with sqlite3.connect(db) as conn:
        result = pd.read_sql(
            "SELECT * FROM engagements WHERE player_steamid=76561198386265483 AND map_name='de_ancient'",
            conn,
        )
    assert len(result) == 1
    assert result.iloc[0]["map_name"] == "de_ancient"
```
  </action>
  <verify>
    <automated>cd "D:/Obsidian/opacity/40_Projects/cs2-ddm" && python -m pytest -p no:cov 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -p no:cov 2>&1 | grep -v "^#" | tail -1` содержит "passed" и НЕ содержит "failed" или "error"
    - Число собранных тестов >= 215: `python -m pytest --collect-only -q 2>&1 | grep "tests collected"` показывает >= 215
    - `python -m pytest -k "t0_min_offset" -p no:cov 2>&1 | tail -3` — >= 1 passed (SC1 regression guard)
    - `python -m pytest -k "Overlapping" -p no:cov 2>&1 | tail -3` — >= 5 passed
    - `python -m pytest -k "TestTeammateGate or teammate" -p no:cov 2>&1 | tail -3` — >= 4 passed
    - `python -m pytest tests/test_db_utils.py -p no:cov 2>&1 | tail -3` — все passed
  </acceptance_criteria>
  <done>Весь suite зелёный, count >= 215, все Phase 6 SC покрыты тестами включая T0_MIN_OFFSET_TICKS edge case с конкретными assertions</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Все изменения Phase 6 реализованы и протестированы:
    - Plan 01: overlapping window gate + teammate phantom kill gate + player_steamid в return dict analyze_engagement_episode()
    - Plan 02: player_steamid в DuelAttempt + save_attempts() dedup с itertools.groupby + match_id обязателен (нет дефолта)
    - Plan 03: db_utils.py + SQLite dual-write в обоих output paths + test_csv_sqlite_row_parity
    - Plan 04: regression suite зелёный, T0_MIN_OFFSET_TICKS тест с parametrize существует
  </what-built>
  <how-to-verify>
    1. Запустить `python -m pytest -p no:cov 2>&1 | tail -5` — убедиться что все тесты проходят, count >= 215
    2. Запустить `python -c "import db_utils; print('db_utils OK')"` — должно вывести "db_utils OK"
    3. Запустить `python -c "from duel_attempts import DuelAttempt; import dataclasses; a = DuelAttempt('m','de_dust2',1,2,True,1,1,'peek',50.0,5.0); print(dataclasses.asdict(a).get('player_steamid'))"` — должно вывести `None` (дефолт)
    4. Запустить `python -c "from ddm_analyzer import DDMAnalyzer; import inspect; src = inspect.getsource(DDMAnalyzer.__init__); print('last_accepted_t2_tick' in src)"` — должно вывести `True`
    5. Запустить `python -m pytest -k "t0_min_offset" -p no:cov -v` — должен быть >= 1 passed (Edge Case 3 regression guard)
    6. Опционально: если есть .dem файл в `for_analysis/`, запустить `python run_analysis.py` и убедиться что `analytics.db` создаётся
  </how-to-verify>
  <resume-signal>Напишите "approved" если всё работает, или опишите проблему для диагностики</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-|-|
| test suite → production code | Тесты верифицируют что production code соответствует Phase 6 контрактам |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-|-|-|-|-|
| T-06-08 | Repudiation | Phase 6 SC compliance | mitigate | Автоматические тесты документируют каждый SC — audit trail через pytest output |
</threat_model>

<verification>
```bash
cd "D:/Obsidian/opacity/40_Projects/cs2-ddm"
python -m pytest -p no:cov 2>&1 | tail -5
python -m pytest --collect-only -q 2>&1 | grep "tests collected"
python -m pytest -k "t0_min_offset" -p no:cov -v 2>&1 | tail -5
```
</verification>

<success_criteria>
- Все 215+ тестов зелёные (0 failures, 0 errors)
- Phase 6 SC 1-4 имеют автоматическое тестовое покрытие
- Тест `test_t0_min_offset_ticks_gate` существует в test_t0_detector_find_t0.py с parametrize: offset 0,19→None; offset 20,21→not None
- Checkpoint: пользователь подтвердил ручную верификацию
</success_criteria>

<output>
После завершения создать `.planning/phases/06-quality-gates-schema-migration/06-04-SUMMARY.md`
</output>
