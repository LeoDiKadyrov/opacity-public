---
phase: 06-quality-gates-schema-migration
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01-PLAN"
  - "06-02-PLAN"
files_modified:
  - db_utils.py
  - ddm_analyzer.py
  - kill_rate_analysis.py
autonomous: true
requirements:
  - "SC3: Both output schemas include player_steamid column — queries like 'all donk moments on de_ancient' work via SQL"

must_haves:
  truths:
    - "db_utils.py существует с функциями save_to_db() и _table_exists()"
    - "analyze_demo() пишет результаты в analytics.db таблицу engagements (dual-write)"
    - "save_attempts() пишет результаты в analytics.db таблицу duel_attempts (dual-write)"
    - "Повторный запуск pipeline удаляет старые строки match_id в SQLite и вставляет новые (идемпотентность)"
    - "SQL запрос SELECT * FROM engagements WHERE player_steamid=X AND map_name='de_ancient' возвращает строки"
    - "analyze_engagement_episode() включает player_steamid в возвращаемый dict (выполнено в Plan 01 Task 3)"
    - "CSV и SQLite содержат идентичные строки для одного и того же DataFrame (SC4)"
  artifacts:
    - path: "db_utils.py"
      provides: "SQLite dual-write utility"
      contains: "save_to_db"
    - path: "analytics.db"
      provides: "SQLite база с engagements и duel_attempts таблицами"
  key_links:
    - from: "ddm_analyzer.analyze_demo()"
      to: "db_utils.save_to_db()"
      via: "вызов после csv_utils.save_results()"
      pattern: "save_to_db.*engagements"
    - from: "kill_rate_analysis.save_attempts()"
      to: "db_utils.save_to_db()"
      via: "вызов после combined.to_csv()"
      pattern: "save_to_db.*duel_attempts"
---

<objective>
Создать `db_utils.py` и подключить SQLite dual-write в обоих output путях.

Purpose: D-01/D-02/D-03 — SQLite аналитика рядом с CSV без изменения Streamlit pipeline. После этого плана `analytics.db` можно запрашивать: `SELECT * FROM engagements WHERE player_steamid=76561198386265483 AND map_name='de_ancient'`.

Output: `db_utils.py` (новый файл), изменения в `ddm_analyzer.py` и `kill_rate_analysis.py` для dual-write.
</objective>

<execution_context>
@D:/Obsidian/opacity/40_Projects/cs2-ddm/.claude/workflows/execute-plan.md
</execution_context>

<context>
@D:/Obsidian/opacity/40_Projects/cs2-ddm/.planning/phases/06-quality-gates-schema-migration/06-CONTEXT.md
@D:/Obsidian/opacity/40_Projects\cs2-ddm\.planning\phases\06-quality-gates-schema-migration\06-PATTERNS.md

<interfaces>
<!-- db_utils.py — целевые сигнатуры из PATTERNS.md -->

```python
def save_to_db(df: pd.DataFrame, db_path: str, table: str, match_id: int | str) -> None:
    """Replace-or-append: удаляет строки match_id, вставляет новые."""

def _table_exists(conn: sqlite3.Connection, table: str) -> bool:
    """True если таблица существует в БД."""
```

<!-- Точки вставки в ddm_analyzer.py (после Plan 01 изменений) -->

analyze_demo() — место вставки после csv_utils.save_results() call.
Нужно найти строку вида: `save_results(results_df, CSV_OUTPUT_PATH, match_id=self.match_id)`
и после неё добавить: `db_utils.save_to_db(results_df, DB_PATH, "engagements", self.match_id)`

<!-- kill_rate_analysis.py — после combined.to_csv() в save_attempts() -->

После `combined.to_csv(path, index=False)` в save_attempts() добавить:
`db_utils.save_to_db(combined, DB_PATH, "duel_attempts", match_id)`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать db_utils.py</name>
  <files>db_utils.py, tests/test_db_utils.py</files>
  <read_first>
    - csv_utils.py (полный файл — аналог для db_utils, lines 1-60)
    - config.py (ищем DB_PATH константу или нужно её добавить)
  </read_first>
  <behavior>
    - Test 1: _table_exists() возвращает False для несуществующей таблицы
    - Test 2: _table_exists() возвращает True после создания таблицы
    - Test 3: save_to_db() создаёт таблицу и вставляет строки при первом вызове
    - Test 4: save_to_db() с тем же match_id заменяет старые строки (idempotency)
    - Test 5: save_to_db() с другим match_id добавляет строки (не перезаписывает)
    - Test 6: save_to_db() при ошибке чтения БД печатает Warning и возвращает (не крашит)
    - Test 7: после save_to_db() можно сделать SQL-запрос по player_steamid
    - Test 8: test_csv_sqlite_row_parity — один DataFrame через save_results() (CSV) и save_to_db() (SQLite) → pd.read_csv() vs pd.read_sql("SELECT * FROM engagements", conn) → assert column sets равны, assert row counts равны (SC4)
  </behavior>
  <action>
**Шаг 1 — написать тесты RED** в `tests/test_db_utils.py` используя `tmp_path` фикстуру pytest.

Для Test 8 (CSV/SQLite parity) — добавить:

```python
def test_csv_sqlite_row_parity(tmp_path):
    """SC4: CSV and SQLite outputs contain identical rows for the same DataFrame."""
    import sqlite3
    import pandas as pd
    from db_utils import save_to_db
    from csv_utils import save_results  # или аналогичный import

    db = str(tmp_path / "test.db")
    csv_path = str(tmp_path / "test.csv")

    df = pd.DataFrame({
        "match_id": ["match1", "match1"],
        "player_steamid": [76561198386265483, 76561198386265483],
        "map_name": ["de_ancient", "de_ancient"],
        "rt_visible_to_hit_ms": [250.0, 310.0],
    })

    # Write via both paths
    df.to_csv(csv_path, index=False)  # CSV path
    save_to_db(df, db, "engagements", "match1")  # SQLite path

    # Read back both
    csv_result = pd.read_csv(csv_path)
    with sqlite3.connect(db) as conn:
        db_result = pd.read_sql("SELECT * FROM engagements", conn)

    # Assert parity: same columns, same row count
    assert set(csv_result.columns) == set(db_result.columns), (
        f"Column mismatch: CSV={set(csv_result.columns)}, DB={set(db_result.columns)}"
    )
    assert len(csv_result) == len(db_result), (
        f"Row count mismatch: CSV={len(csv_result)}, DB={len(db_result)}"
    )
```

**Шаг 2 — добавить DB_PATH в `config.py`:**

Проверить `config.py` на наличие `DB_PATH`. Если нет — добавить после существующих PATH констант:
```python
DB_PATH: str = "analytics.db"
```

**Шаг 3 — создать `db_utils.py`:**

```python
"""SQLite dual-write utility for analytics.db (D-01/D-02/D-03).

Analog of csv_utils.save_results(): replace-or-append by match_id.
CSV remains the primary output; this is a parallel write for SQL queries.
"""
import sqlite3
from typing import Union

import pandas as pd


def save_to_db(
    df: pd.DataFrame,
    db_path: str,
    table: str,
    match_id: Union[int, str],
) -> None:
    """Replace-or-append: delete rows for match_id, then insert df rows.

    Matches csv_utils.save_results() idempotency semantics (D-03).
    """
    if df.empty:
        return
    try:
        with sqlite3.connect(db_path) as conn:
            if _table_exists(conn, table):
                conn.execute(
                    f"DELETE FROM {table} WHERE match_id = ?",
                    (str(match_id),),
                )
            df.to_sql(table, conn, if_exists="append", index=False)
    except (sqlite3.DatabaseError, OSError, ValueError) as e:
        print(f"Warning: could not write to '{db_path}' table '{table}': {e}")


def _table_exists(conn: sqlite3.Connection, table: str) -> bool:
    """True if table exists in the database."""
    cur = conn.execute(
        "SELECT name FROM sqlite_master WHERE type='table' AND name=?",
        (table,),
    )
    return cur.fetchone() is not None
```
  </action>
  <verify>
    <automated>cd "D:/Obsidian/opacity/40_Projects/cs2-ddm" && python -m pytest tests/test_db_utils.py -v -p no:cov 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `db_utils.py` существует
    - `grep -c "def save_to_db\|def _table_exists" db_utils.py` выводит 2
    - `grep -c "DB_PATH" config.py` выводит >= 1
    - Все тесты в `tests/test_db_utils.py` проходят включая test_csv_sqlite_row_parity
    - Idempotency test: двойной вызов save_to_db с тем же match_id → в таблице N строк, не 2N
    - `python -m pytest -k "test_csv_sqlite_row_parity" -p no:cov 2>&1 | tail -3` выходит 0 (SC4)
  </acceptance_criteria>
  <done>db_utils.py создан, save_to_db() идемпотентна, тесты зелёные включая CSV/SQLite parity тест</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Подключить dual-write в analyze_demo() и save_attempts()</name>
  <files>ddm_analyzer.py, kill_rate_analysis.py</files>
  <read_first>
    - ddm_analyzer.py — найти все вызовы save_results() через grep, прочитать окружающий контекст (±10 строк)
    - kill_rate_analysis.py lines 83-95 (save_attempts после Plan 02 изменений)
    - config.py (DB_PATH константа — должна быть добавлена в Task 1)
  </read_first>
  <behavior>
    - Test 1: analyze_demo() вызывает db_utils.save_to_db после save_results() — проверить через `unittest.mock.patch('ddm_analyzer.db_utils.save_to_db')` (или `unittest.mock.patch('db_utils.save_to_db')`), убедиться что mock вызван с table="engagements" и match_id=self.match_id
    - Test 2: save_attempts() вызывает db_utils.save_to_db после combined.to_csv() — проверить через `unittest.mock.patch.object(kill_rate_analysis, 'db_utils')`, убедиться что mock.save_to_db.assert_called_once() и аргумент table == "duel_attempts"
    - Test 3: если db_utils.save_to_db бросает исключение — pipeline не крашится (covered by save_to_db's own try/except)
  </behavior>
  <action>
**Шаг 1 — написать тесты RED** в `tests/test_db_utils.py` (или новый `tests/test_dual_write.py`):

```python
from unittest.mock import patch, MagicMock
import pandas as pd

def test_analyze_demo_calls_save_to_db(tmp_path, monkeypatch):
    """dual-write: analyze_demo() должен вызывать db_utils.save_to_db для engagements."""
    # Импортировать DDMAnalyzer и патчить db_utils.save_to_db + csv_utils.save_results
    # Проверить что mock вызван с table="engagements" и match_id равен self.match_id
    pass  # реализацию написать в RED фазе

def test_save_attempts_calls_save_to_db(tmp_path):
    """dual-write: save_attempts() должен вызывать db_utils.save_to_db для duel_attempts."""
    from duel_attempts import DuelAttempt
    import kill_rate_analysis
    attempt = DuelAttempt('match1', 'de_dust2', 100, 123, True, 5, 3, 'peek', 100.0, 5.0)
    with patch.object(kill_rate_analysis, 'db_utils') as mock_db:
        kill_rate_analysis.save_attempts("test", [attempt], match_id="match1")
        mock_db.save_to_db.assert_called_once()
        call_args = mock_db.save_to_db.call_args
        assert call_args[0][2] == "duel_attempts"  # table arg positional
```

**Шаг 2 — добавить импорты в `ddm_analyzer.py`:**

Добавить в начало файла (рядом с csv_utils импортом):
```python
import db_utils
from config import DB_PATH
```

**Шаг 3 — dual-write в `analyze_demo()`:**

Найти место где вызывается `csv_utils.save_results()` или `save_results()` в `analyze_demo()`.
После этого вызова добавить:
```python
# SQLite dual-write (D-01)
db_utils.save_to_db(results_df, DB_PATH, "engagements", self.match_id)
```

**Шаг 4 — импорты и dual-write в `kill_rate_analysis.py`:**

1. Добавить импорт:
```python
import db_utils
from config import DB_PATH
```

2. В `save_attempts()` после строки `combined.to_csv(path, index=False)` добавить:
```python
# SQLite dual-write (D-01/D-02)
db_utils.save_to_db(combined, DB_PATH, "duel_attempts", match_id)
```
  </action>
  <verify>
    <automated>cd "D:/Obsidian/opacity/40_Projects/cs2-ddm" && python -m pytest tests/ -k "dual_write or save_to_db or DualWrite" -v -p no:cov 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "db_utils.save_to_db" ddm_analyzer.py` выводит >= 1
    - `grep -c "db_utils.save_to_db" kill_rate_analysis.py` выводит >= 1
    - `grep -c "import db_utils" ddm_analyzer.py` выводит 1
    - `grep -c "import db_utils" kill_rate_analysis.py` выводит 1
    - `grep -n '"player_steamid".*self.player_steamid\|self.player_steamid.*player_steamid' ddm_analyzer.py` выводит строку из analyze_engagement_episode() return dict (добавлено Plan 01 Task 3)
    - mock-тесты для dual-write проходят: save_to_db вызывается с правильными именами таблиц
    - `python -m pytest -p no:cov 2>&1 | tail -3` — все тесты зелёные
  </acceptance_criteria>
  <done>analyze_demo() и save_attempts() пишут в analytics.db параллельно с CSV; player_steamid присутствует в return dict (Plan 01) и попадает в engagements таблицу</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-|-|
| DataFrame → SQLite | Данные из demoparser2 — возможны NaN, inf, нетипичные значения |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-|-|-|-|-|
| T-06-05 | Tampering | analytics.db file | accept | Локальный файл на машине разработчика, не production exposure |
| T-06-06 | Denial | sqlite3.DatabaseError | mitigate | try/except в save_to_db() — Warning + return, не crash pipeline |
| T-06-07 | Spoofing | SQL table name injection | accept | table имена — "engagements" / "duel_attempts" — hardcoded string literals, не user input |
</threat_model>

<verification>
```bash
cd "D:/Obsidian/opacity/40_Projects/cs2-ddm"
python -m pytest tests/test_db_utils.py -v -p no:cov
python -m pytest -k "test_csv_sqlite_row_parity" -p no:cov 2>&1 | tail -3
python -m pytest -p no:cov 2>&1 | tail -3
python -c "import db_utils; print('import OK')"
grep "DB_PATH" config.py
grep '"player_steamid"' ddm_analyzer.py | grep "self.player_steamid"
```
</verification>

<success_criteria>
- db_utils.py создан с save_to_db() и _table_exists()
- analytics.db создаётся при первом pipeline запуске с таблицами engagements и duel_attempts
- player_steamid присутствует в return dict analyze_engagement_episode() (Plan 01 Task 3) и в обеих таблицах
- mock-тесты подтверждают что dual-write вызовы происходят с правильными аргументами
- test_csv_sqlite_row_parity проходит: CSV и SQLite содержат идентичные column sets и row counts (SC4)
- Все тесты (215+) зелёные
- SQL запрос `SELECT player_steamid, map_name FROM engagements LIMIT 5` возвращает данные после pipeline запуска
</success_criteria>

<output>
После завершения создать `.planning/phases/06-quality-gates-schema-migration/06-03-SUMMARY.md`
</output>
