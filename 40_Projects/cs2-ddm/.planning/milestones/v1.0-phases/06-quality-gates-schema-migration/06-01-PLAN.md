---
phase: 06-quality-gates-schema-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ddm_analyzer.py
autonomous: true
requirements:
  - "SC1: RT > 3000ms never produced due to T0-at-search-start (already done via D-10)"
  - "SC2: Two engagements < 300 ticks apart never produce duplicate moment for same enemy"
  - "SC4: All 215 existing tests remain green"

must_haves:
  truths:
    - "analyze_demo() rejects a moment whose first_hit_tick is within 300 ticks of the previous accepted t2_first_hit_tick"
    - "analyze_demo() logs a WARNING for every rejected overlapping window moment"
    - "analyze_engagement_episode() rejects a moment if any non-player attacker hurts the target enemy in [T0..T2]"
    - "analyze_engagement_episode() logs a WARNING for every teammate phantom kill rejection"
    - "DDMAnalyzer.__init__ signature accepts player_steamid parameter (D-04)"
    - "analyze_engagement_episode() return dict contains key player_steamid with value self.player_steamid"
  artifacts:
    - path: "ddm_analyzer.py"
      provides: "last_accepted_t2_tick state + overlapping window gate + _teammate_hurt_target() method + player_steamid in return dict"
      contains: "last_accepted_t2_tick"
  key_links:
    - from: "analyze_demo() loop"
      to: "last_accepted_t2_tick"
      via: "check before results.append(), update after accept"
      pattern: "last_accepted_t2_tick"
    - from: "analyze_engagement_episode()"
      to: "_teammate_hurt_target()"
      via: "call after is_1v1_duel(), before velocity gate"
      pattern: "_teammate_hurt_target"
    - from: "analyze_engagement_episode() return dict"
      to: "results_df column player_steamid"
      via: "player_steamid key in returned dict"
      pattern: "\"player_steamid\".*self\\.player_steamid"
---

<objective>
Добавить два quality gate в DDMAnalyzer: overlapping window gate (D-07/D-08) и teammate gate (D-09). Также добавить `player_steamid` в return dict `analyze_engagement_episode()` (Path 1 schema, D-05).

Purpose: Устранить дублирование моментов при близких engagement'ах и "phantom kill" когда тиммейт помогает убить цель — оба дефекта загрязняют данные которые Phase 8 будет интерпретировать. player_steamid в return dict необходим для SQL запросов после dual-write (Plan 03).

Output: ddm_analyzer.py с `last_accepted_t2_tick` state, overlapping window check в `analyze_demo()`, `_teammate_hurt_target()` методом в `analyze_engagement_episode()`, и `player_steamid` в return dict.
</objective>

<execution_context>
@D:/Obsidian/opacity/40_Projects/cs2-ddm/.claude/workflows/execute-plan.md
</execution_context>

<context>
@D:/Obsidian/opacity/40_Projects/cs2-ddm/.planning/phases/06-quality-gates-schema-migration/06-CONTEXT.md
@D:/Obsidian/opacity/40_Projects/cs2-ddm/.planning/phases/06-quality-gates-schema-migration/06-PATTERNS.md

<interfaces>
<!-- Extracted from ddm_analyzer.py — ключевые точки вставки -->

__init__ state block (lines 64-72):
```python
self.demo_path = demo_path
self.player_steamid = player_steamid
self.tickrate = tickrate
self.debug_prints = debug_prints
self.match_id = match_id if match_id is not None else self._hash_demo(demo_path)
self.logger = get_logger(self.match_id, debug_prints)
self.analysis_moments: List[AnalysisMoment] = []
self.enemy_velocity_threshold = enemy_velocity_threshold
self.player_velocity_threshold = player_velocity_threshold
```

analyze_engagement_episode insertion point (lines 606-618):
```python
is_clean, reason = self.is_1v1_duel(all_player_hurt_events_df, t0_tick, t2_tick, target_enemy_id)
if not is_clean:
    self.logger.warning(f"{tag} REJECTED — not a clean 1v1: {reason}")
    return None

enemy_vel = self._compute_velocity(int(target_enemy_id), t0_tick, all_ticks_df)
```

analyze_demo() results loop (lines 772-779):
```python
results = []
for moment in self.analysis_moments:
    result = self.analyze_engagement_episode(
        moment, all_ticks_df, player_fire_df, all_hurt_df,
        round_start_ticks, smoke_events=smoke_events,
    )
    if result:
        results.append(result)
```

is_1v1_duel signature (analog for _teammate_hurt_target):
```python
def is_1v1_duel(
    self,
    all_player_hurt_events_df: pd.DataFrame,
    t0_tick: int,
    t2_tick: int,
    target_enemy_id,
) -> Tuple[bool, str]:
```

logger.warning rejection pattern (line 608):
```python
self.logger.warning(f"{tag} REJECTED — not a clean 1v1: {reason}")
return None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Overlapping window gate (D-07, D-08)</name>
  <files>ddm_analyzer.py, tests/test_ddm_analyzer_quality.py</files>
  <read_first>
    - ddm_analyzer.py (полный файл — нужны lines 48-87 для __init__, lines 772-796 для analyze_demo loop)
    - tests/test_ddm_analyzer_quality.py (понять существующие фикстуры и паттерны тестов)
  </read_first>
  <behavior>
    - Test 1: момент с first_hit_tick = last_accepted_t2_tick + 299 ОТВЕРГАЕТСЯ (< +300)
    - Test 2: момент с first_hit_tick = last_accepted_t2_tick + 300 ПРИНИМАЕТСЯ (граница включительно: 300 == 300 не rejected)
    - Test 3: момент с first_hit_tick = last_accepted_t2_tick + 301 ПРИНИМАЕТСЯ
    - Test 4: первый момент всегда принимается (last_accepted_t2_tick is None)
    - Test 5: после принятия момента last_accepted_t2_tick обновляется до t2_first_hit_tick
    - Test 6: rejected момент не обновляет last_accepted_t2_tick
    - Test 7: logger.warning вызывается при rejection с текстом "Overlapping window rejected"
  </behavior>
  <action>
**Шаг 1 — написать тесты RED (до реализации):**

В `tests/test_ddm_analyzer_quality.py` добавить класс `TestOverlappingWindowGate` с тестами через `analyzer.last_accepted_t2_tick` и мокированием `analyze_engagement_episode`.

**Шаг 2 — реализация в `ddm_analyzer.py`:**

В `__init__` после строки `self.player_velocity_threshold = player_velocity_threshold` (line 72) добавить:
```python
self.last_accepted_t2_tick: Optional[int] = None  # overlapping window gate (D-07)
```

В `analyze_demo()` заменить блок (lines 778-779):
```python
if result:
    results.append(result)
```
на:
```python
if result:
    first_hit = result.get("t2_first_hit_tick")
    if (
        self.last_accepted_t2_tick is not None
        and isinstance(first_hit, (int, float))
        and not math.isnan(float(first_hit))
        and int(first_hit) < self.last_accepted_t2_tick + 300
    ):
        self.logger.warning(
            f"Overlapping window rejected: first_hit={first_hit} < "
            f"last_accepted_t2={self.last_accepted_t2_tick} + 300"
        )
    else:
        results.append(result)
        if isinstance(first_hit, (int, float)) and not math.isnan(float(first_hit)):
            self.last_accepted_t2_tick = int(first_hit)
```

Убедиться что `import math` уже есть в начале файла (он есть).
  </action>
  <verify>
    <automated>cd "D:/Obsidian/opacity/40_Projects/cs2-ddm" && python -m pytest tests/test_ddm_analyzer_quality.py -k "Overlapping" -v -p no:cov 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "last_accepted_t2_tick" ddm_analyzer.py` выводит >= 3 (объявление в __init__, чтение в gate, обновление после accept)
    - `grep -c "Overlapping window rejected" ddm_analyzer.py` выводит 1
    - Все тесты класса TestOverlappingWindowGate проходят (зелёные)
  </acceptance_criteria>
  <done>analyze_demo() реджектит overlapping моменты, логирует WARNING, обновляет state после принятия</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Teammate phantom kill gate (D-09)</name>
  <files>ddm_analyzer.py, tests/test_ddm_analyzer_quality.py</files>
  <read_first>
    - ddm_analyzer.py lines 251-295 (is_1v1_duel — аналог для _teammate_hurt_target)
    - ddm_analyzer.py lines 578-618 (analyze_engagement_episode — точка вставки)
  </read_first>
  <behavior>
    - Test 1: пустой DataFrame hurt событий → метод возвращает False (нет тиммейта)
    - Test 2: только player атакует target в [T0..T2] → False (не тиммейт)
    - Test 3: attacker_steamid != player_steamid И != target_enemy_id атакует target в [T0..T2] → True (phantom kill)
    - Test 4: событие за пределами окна (tick < t0 или tick > t2) → False
    - Test 5: analyze_engagement_episode с phantom kill возвращает None
    - Test 6: logger.warning вызывается с текстом "teammate phantom kill"
  </behavior>
  <action>
**Шаг 1 — написать тесты RED:**

В `tests/test_ddm_analyzer_quality.py` добавить класс `TestTeammateGate`.

**Шаг 2 — добавить `_teammate_hurt_target()` в `ddm_analyzer.py`:**

После метода `is_1v1_duel()` (примерно после line 290) добавить:
```python
def _teammate_hurt_target(
    self,
    all_player_hurt_events_df: pd.DataFrame,
    t0_tick: int,
    t2_tick: int,
    target_enemy_id,
) -> bool:
    """True если в окне [t0..t2] кто-то кроме player нанёс урон target_enemy_id.

    Phase 6 simplified: любой attacker != player_steamid считается тиммейтом.
    Точная team_num фильтрация — Phase 8 refinement.
    """
    if all_player_hurt_events_df.empty:
        return False
    required_cols = {"tick", "user_steamid", "attacker_steamid"}
    if not required_cols.issubset(all_player_hurt_events_df.columns):
        return False
    window = all_player_hurt_events_df[
        (all_player_hurt_events_df["tick"] >= t0_tick)
        & (all_player_hurt_events_df["tick"] <= t2_tick)
        & (all_player_hurt_events_df["user_steamid"].astype(str) == str(target_enemy_id))
        & (all_player_hurt_events_df["attacker_steamid"].astype(str) != str(self.player_steamid))
    ]
    return not window.empty
```

**Шаг 3 — вставить gate в `analyze_engagement_episode()`:**

После строки `if not is_clean:` блока (после line 609, до `enemy_vel =` line 611) добавить:
```python
# Teammate gate (D-09): reject if phantom kill detected
if self._teammate_hurt_target(all_player_hurt_events_df, t0_tick, t2_tick, target_enemy_id):
    self.logger.warning(f"{tag} REJECTED — teammate phantom kill detected in [T0..T2]")
    return None
```
  </action>
  <verify>
    <automated>cd "D:/Obsidian/opacity/40_Projects/cs2-ddm" && python -m pytest tests/test_ddm_analyzer_quality.py -k "Teammate" -v -p no:cov 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_teammate_hurt_target" ddm_analyzer.py` выводит >= 2 (определение + вызов)
    - `grep -c "teammate phantom kill" ddm_analyzer.py` выводит 1
    - Все тесты класса TestTeammateGate проходят
    - `python -m pytest tests/test_ddm_analyzer_quality.py -p no:cov` — все тесты зелёные (включая 12 старых + новые)
    - `grep "def __init__.*player_steamid\|player_steamid.*:" ddm_analyzer.py | head -3` показывает что player_steamid присутствует в сигнатуре __init__ (D-04). Если параметра нет — добавить `player_steamid: int` в __init__ и `self.player_steamid = player_steamid` в тело.
    - `grep -n "self\.player_steamid" ddm_analyzer.py | grep "__init__"` выводит строку присваивания в __init__ (D-04 acceptance)
  </acceptance_criteria>
  <done>analyze_engagement_episode() отвергает phantom kill моменты с WARNING в логе; player_steamid присутствует в __init__ (D-04)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: player_steamid в return dict analyze_engagement_episode() (D-05 Path 1)</name>
  <files>ddm_analyzer.py, tests/test_ddm_analyzer_quality.py</files>
  <read_first>
    - ddm_analyzer.py — найти return statement analyze_engagement_episode() через grep: `grep -n "return {" ddm_analyzer.py` или `grep -n "\"rt_visible_to_hit_ms\"" ddm_analyzer.py` для локализации return dict
  </read_first>
  <behavior>
    - Test 1: analyze_engagement_episode() возвращает dict с ключом "player_steamid"
    - Test 2: значение "player_steamid" в dict равно self.player_steamid (не None, не 0)
    - Test 3: при player_steamid=76561198386265483 возвращаемый dict["player_steamid"] == 76561198386265483
  </behavior>
  <action>
**Шаг 1 — написать тесты RED:**

В `tests/test_ddm_analyzer_quality.py` добавить класс `TestAnalyzeEpisodeReturnDict`:
```python
class TestAnalyzeEpisodeReturnDict:
    """Task 3: player_steamid в return dict analyze_engagement_episode() (D-05 Path 1)."""

    def test_return_dict_contains_player_steamid(self, ...):
        """analyze_engagement_episode() возвращает dict с ключом player_steamid."""
        # Создать DDMAnalyzer с player_steamid=76561198386265483
        # Вызвать analyze_engagement_episode() с достаточными данными для успешного прохода
        # Проверить что result["player_steamid"] == 76561198386265483
        ...
```

Использовать существующие фикстуры или создать минимальный набор тестовых данных по образцу других тестов в файле.

**Шаг 2 — добавить поле в return dict:**

Найти return statement в `analyze_engagement_episode()` через:
```bash
grep -n "return {" ddm_analyzer.py
```
Или найти по паттерну `"rt_visible_to_hit_ms"` — он точно присутствует в финальном dict.

В найденный return dict добавить:
```python
"player_steamid": self.player_steamid,  # D-05: Path 1 schema
```

Вставить рядом с другими identity полями (match_id, target_enemy_id и т.п.).
  </action>
  <verify>
    <automated>cd "D:/Obsidian/opacity/40_Projects/cs2-ddm" && python -m pytest tests/test_ddm_analyzer_quality.py -k "ReturnDict" -v -p no:cov 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"player_steamid".*self\.player_steamid\|self\.player_steamid.*"player_steamid"' ddm_analyzer.py` выводит строку из тела analyze_engagement_episode() (не из __init__)
    - Тесты класса TestAnalyzeEpisodeReturnDict проходят
    - `python -m pytest tests/test_ddm_analyzer_quality.py -p no:cov 2>&1 | tail -3` — 0 failures
  </acceptance_criteria>
  <done>analyze_engagement_episode() явно добавляет "player_steamid": self.player_steamid в return dict — колонка попадёт в results_df и далее в CSV и SQLite</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-|-|
| demo data → analyzer | Ticks и hurt events из demoparser2 — untrusted floats/NaN возможны |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-|-|-|-|-|
| T-06-01 | Tampering | last_accepted_t2_tick state | mitigate | NaN guard перед int() cast — уже включён в gate code |
| T-06-02 | Denial | _teammate_hurt_target missing columns | mitigate | required_cols check перед фильтрацией — возвращает False если столбцов нет |
</threat_model>

<verification>
```bash
cd "D:/Obsidian/opacity/40_Projects/cs2-ddm"
python -m pytest tests/test_ddm_analyzer_quality.py -v -p no:cov
python -m pytest -p no:cov 2>&1 | tail -3
grep "def __init__" ddm_analyzer.py | head -3
grep -n "self\.player_steamid" ddm_analyzer.py | grep "__init__"
grep -n '"player_steamid".*self\.player_steamid' ddm_analyzer.py
```
Все 215+ тестов зелёные. Число тестов увеличилось на количество новых (TestOverlappingWindowGate + TestTeammateGate + TestAnalyzeEpisodeReturnDict).
</verification>

<success_criteria>
- ddm_analyzer.py содержит `last_accepted_t2_tick` state и overlapping window rejection в analyze_demo()
- ddm_analyzer.py содержит `_teammate_hurt_target()` метод и вызов в analyze_engagement_episode()
- DDMAnalyzer.__init__ принимает `player_steamid` как параметр и присваивает `self.player_steamid` (D-04)
- analyze_engagement_episode() явно включает `"player_steamid": self.player_steamid` в return dict (D-05 Path 1)
- Все 215 существующих тестов остаются зелёными
- Новые TDD тесты для обоих gate'ов и return dict зелёные
</success_criteria>

<output>
После завершения создать `.planning/phases/06-quality-gates-schema-migration/06-01-SUMMARY.md`
</output>
