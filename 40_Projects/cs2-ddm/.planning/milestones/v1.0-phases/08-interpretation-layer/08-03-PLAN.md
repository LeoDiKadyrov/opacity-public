---
phase: 08-interpretation-layer
plan: 03
type: execute
wave: 3
depends_on: [08-01, 08-02]
files_modified: []
autonomous: false
requirements:
  - REQ-INT-01
  - REQ-INT-02
  - REQ-INT-03
  - REQ-INT-04
  - REQ-INT-05

must_haves:
  truths:
    - "Summary card (worst metric + drill) is the FIRST thing visible in Interpretation section"
    - "Peek and Hold tables are in separate tabs — no cross-contamination"
    - "Survivorship bias caption appears directly under each RT metric row"
    - "Benchmark dropdown shows karrigan (57 demos) without small-sample warning"
    - "Entering karrigan SteamID (76561197989430253) shows populated tier table with no crashes"
  artifacts: []
  key_links: []
---

<objective>
Manual Streamlit verification of the Interpretation section. No code changes — this plan is a structured checklist that the executor runs in the browser and reports outcomes for each success criterion.

Purpose: Automated tests cannot verify Streamlit rendering, widget state, or caption positioning.
Output: Verified SC1–SC5 pass/fail report written to 08-03-SUMMARY.md.
</objective>

<execution_context>
@D:\Obsidian\opacity\40_Projects\cs2-ddm\.claude\get-shit-done\workflows\execute-plan.md
</execution_context>

<context>
@D:\Obsidian\opacity\40_Projects\cs2-ddm\.planning\phases\08-interpretation-layer\08-CONTEXT.md
@D:\Obsidian\opacity\40_Projects\cs2-ddm\.planning\phases\08-interpretation-layer\08-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 (08-03-01): Streamlit — summary card and metric table layout</name>
  <what-built>
    Plan 02 added the Interpretation section to app.py with a summary card above the metric table, benchmark dropdown, and separate Peek/Hold tabs.
  </what-built>
  <how-to-verify>
    1. Run: `streamlit run app.py`
    2. In the sidebar Configuration section, enter SteamID64: `76561197989430253` (karrigan)
    3. Scroll to the "Interpretation" section at the bottom of the page
    4. Verify the following — check each:

       **SC1 — Metric table structure:**
       [ ] Five metric rows visible in Peek tab: crosshair_angle, rt_visible_to_aim_ms, rt_aim_to_hit_ms, rt_visible_to_hit_ms, kill_rate (hit_rate is a bonus)
       [ ] Each row shows: Metric | Your value | Tier (Elite/Good/Average/Work needed) | vs benchmark | Drill text

       **SC2 — Summary card at top:**
       [ ] An info box (blue) appears ABOVE the metric table — not below
       [ ] The card names the single worst metric and one drill
       [ ] Text follows "Your biggest opportunity (peek): `[metric]` is rated [tier]. Drill: [text]"

       **SC3 — Peek/Hold separation:**
       [ ] Two tabs visible: "Peek" and "Hold"
       [ ] Switching to "Hold" tab shows different rows (rt_visible_to_aim_ms and rt_aim_to_hit_ms should show "n/a" or be absent)

       **Benchmark dropdown:**
       [ ] Dropdown shows "karrigan" (not a raw SteamID)
       [ ] No "(small sample)" suffix on karrigan
       [ ] No warning banner about small sample

       **No sidebar SteamID test:**
       [ ] Clear sidebar SteamID field → info message appears ("Enter SteamID64 in sidebar...")
       [ ] No Python traceback in browser
  </how-to-verify>
  <resume-signal>Type "approved" if all checkboxes pass, or describe which items failed with exact text seen in browser</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (08-03-02): Survivorship bias caption inline under RT rows</name>
  <what-built>
    Each RT metric row (rt_visible_to_aim_ms, rt_aim_to_hit_ms, rt_visible_to_hit_ms) should have a small grey caption directly beneath it, not at the bottom of the table.
  </what-built>
  <how-to-verify>
    1. App still running from Task 1. karrigan SteamID still in sidebar.
    2. In Interpretation section, Peek tab:

       **SC4 — Survivorship bias caveat:**
       [ ] Grey caption text visible directly under each RT metric row: "Measured on hits only — survivorship bias applies."
       [ ] Caption appears under rt_visible_to_aim_ms
       [ ] Caption appears under rt_aim_to_hit_ms
       [ ] Caption appears under rt_visible_to_hit_ms
       [ ] Caption does NOT appear under crosshair_angle or kill_rate/hit_rate rows

       **SC5 — RT bottleneck drill-down (if applicable):**
       [ ] If rt_visible_to_hit_ms drill shows "[T0→T1 bottleneck]" or "[T1→T2 bottleneck]" prefix, verify it reads naturally
       [ ] If bottleneck_component is None (components equal), no prefix shown — this is acceptable

       **D-12 small sample test (optional, if you have a player with <20 demos):**
       [ ] Enter any SteamID with <20 demos in analytics.db → dropdown shows "(small sample)" suffix + warning banner appears
       [ ] If no such player available, skip this check and note in response
  </how-to-verify>
  <resume-signal>Type "approved" if captions verified, or describe exact location of missing/wrong captions. Include SC5 bottleneck component observation.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-|-|
| Browser → Streamlit | No new trust boundaries introduced in this plan (verification only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-|-|-|-|-|
| T-08-01 | Injection | Already mitigated in Plans 01–02 | accept | int() cast verified in Plan 01 tests; parameterized SQL verified in integration test 08-02-01 |
</threat_model>

<verification>
Full suite before sign-off:
- `python -m pytest --override-ini="addopts=--strict-markers" -q` → 289+ passed, 0 failed
- `python -m pytest tests/test_interpretation.py -p no:cov -q` → 11 passed (10 unit + 1 integration)
- Both manual checkpoints approved by user
</verification>

<success_criteria>
All 5 Phase 8 success criteria from ROADMAP.md confirmed TRUE:
1. SC1: Metric table shows value | tier | benchmark | gap | drill for 5 metrics
2. SC2: User can identify single worst metric and one drill from summary card
3. SC3: Peek and hold always separate — confirmed via tabs
4. SC4: Survivorship bias caveat inline under RT rows — confirmed visually
5. SC5: Composite RT drills down to bottleneck component — confirmed or N/A noted
</success_criteria>

<output>
After completion, create `.planning/phases/08-interpretation-layer/08-03-SUMMARY.md`

Include in the summary:
- Pass/fail for each SC1–SC5
- Any issues found and whether they were fixed inline
- Final test count
</output>
