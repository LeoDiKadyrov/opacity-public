---
phase: 09.1
plan: 01
type: execute
wave: 1
depends_on: ["09.1-00"]
files_modified:
  - t0_detector.py
autonomous: true
requirements: [SC1]

must_haves:
  truths:
    - "AABB center ray (0,0,0.5) is checked first inside per-tick loop"
    - "find_t0 result tick is unchanged (semantics preserved)"
    - "test_aabb_offsets_center_first PASSES (was RED in Wave 0)"
  artifacts:
    - path: "t0_detector.py"
      provides: "Reordered _AABB_OFFSETS list — center mass index 0"
      contains: "(0.0, 0.0, 0.5)"
  key_links:
    - from: "t0_detector.py"
      to: "_AABB_OFFSETS[0]"
      via: "tuple literal"
      pattern: "_AABB_OFFSETS"
---

<objective>
SC1: Reorder `_AABB_OFFSETS` so the center-mass offset `(0.0, 0.0, 0.5)` is index 0. Most-visible case exits the corner loop earliest.

Purpose: Cheap-first per D-03. Pure rearrangement — no semantic change (any-corner-visible remains the rule).
Output: t0_detector.py with center first; SC1 RED test turns GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-demo-analysis-perf-pass/09.1-CONTEXT.md
@.planning/phases/09.1-demo-analysis-perf-pass/09.1-RESEARCH.md
@.planning/phases/09.1-demo-analysis-perf-pass/09.1-PATTERNS.md
@CLAUDE.md
@t0_detector.py

<interfaces>
From RESEARCH.md §Pattern 4 (verbatim target shape):

```python
_AABB_OFFSETS: List[Tuple[float, float, float]] = [
    (0.0,                0.0,                0.5),  # center mass — most-likely-visible, check first
    (-_HULL_HALF_WIDTH, -_HULL_HALF_WIDTH, 0.2),
    ( _HULL_HALF_WIDTH, -_HULL_HALF_WIDTH, 0.2),
    # ... rest unchanged from current order
]
```

Correctness: `find_t0` returns on first successful ray; reorder only changes intra-tick check order, never the returned `t0_tick`. Semantics preserved.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move center-mass offset to _AABB_OFFSETS[0]</name>
  <files>t0_detector.py</files>
  <read_first>
    - t0_detector.py (current _AABB_OFFSETS at lines 45-55)
    - .planning/phases/09.1-demo-analysis-perf-pass/09.1-RESEARCH.md §Pattern 4
    - tests/test_t0_detector_find_t0.py (the 3 RED tests added in Wave 0)
  </read_first>
  <behavior>
    - `_AABB_OFFSETS[0]` is exactly `(0.0, 0.0, 0.5)`
    - All other tuples in the list remain present (same set), just reordered
    - len(_AABB_OFFSETS) unchanged
  </behavior>
  <action>
    Open t0_detector.py. Locate the `_AABB_OFFSETS` list literal (currently lines 45-55, where center `(0.0, 0.0, 0.5)` is at the END). Move that center tuple to index 0, leaving every other tuple in its current relative order. Add an inline comment on the center line: `# center mass — most-likely-visible, check first`. Do NOT change `_HULL_HALF_WIDTH` or any other constant. Do NOT alter `find_t0` body or any other code in the file.
  </action>
  <verify>
    <automated>python -m pytest tests/test_t0_detector_find_t0.py -k "center_first" -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from t0_detector import _AABB_OFFSETS; assert _AABB_OFFSETS[0] == (0.0, 0.0, 0.5), _AABB_OFFSETS[0]; print('OK')"` prints `OK`
    - `python -c "from t0_detector import _AABB_OFFSETS; assert len(_AABB_OFFSETS) == 9 or len(_AABB_OFFSETS) >= 8; print(len(_AABB_OFFSETS))"` matches the prior length (no entries dropped)
    - `python -m pytest tests/test_t0_detector_find_t0.py -k "center_first" -x` PASSES
    - Full file test suite still green: `python -m pytest tests/test_t0_detector_find_t0.py -x` (every test passes)
  </acceptance_criteria>
  <done>SC1 GREEN; _AABB_OFFSETS[0] is center; full t0_detector test file green.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_t0_detector_find_t0.py -x` all green
- Manual: `python -c "from t0_detector import _AABB_OFFSETS; print(_AABB_OFFSETS[0])"` → `(0.0, 0.0, 0.5)`
- Recommend running `/check-phase6` skill before commit per project convention (t0_detector.py edge case checklist).
</verification>

<success_criteria>
SC1 RED test now GREEN; no other tests regressed.
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-demo-analysis-perf-pass/09.1-01-SUMMARY.md`. Commit message: `perf(09.1): center-ray first in _AABB_OFFSETS (SC1)`.
</output>
