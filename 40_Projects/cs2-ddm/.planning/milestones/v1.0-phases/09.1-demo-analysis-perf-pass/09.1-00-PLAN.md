---
phase: 09.1
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_ddm_analyzer_core.py
  - tests/test_t0_detector_find_t0.py
  - bench/run_bench.py
  - bench/README.md
autonomous: true
requirements: [SC1, SC2, SC3, SC4, SC5]

must_haves:
  truths:
    - "RED tests exist for SC1 (center-ray ordering), SC2 (parse_events batch), SC3 (selective parse_ticks + empty-anchor guard + flag-off), SC4 (ticks_by_sid kwarg + fallback)"
    - "fake_parser fixture supports parse_events.return_value = [(name, df), ...] shape"
    - "bench/run_bench.py runs cold (subprocess per demo) and writes bench_results.json"
    - "Baseline bench_results.json captured before any production change"
  artifacts:
    - path: "bench/run_bench.py"
      provides: "Cold-cache 10-demo benchmark harness"
    - path: "bench/baseline.json"
      provides: "Pre-change walltime + per-step baseline"
    - path: "tests/test_ddm_analyzer_core.py"
      provides: "RED tests for SC2/SC3/SC4 (parse_events batch, selective ticks, ticks_by_sid)"
      contains: "parse_events_batch"
    - path: "tests/test_t0_detector_find_t0.py"
      provides: "RED tests for SC1 (center-first) + SC4 (ticks_by_sid)"
      contains: "center_first"
  key_links:
    - from: "tests/conftest.py"
      to: "fake_parser fixture"
      via: "parse_events attribute"
      pattern: "parse_events"
    - from: "bench/run_bench.py"
      to: "for_analysis/*.dem"
      via: "subprocess.run"
      pattern: "subprocess"
---

<objective>
Lay test scaffolding (RED phase per D-03 TDD) and benchmark harness (D-01) before any production code change. This plan produces failing tests that subsequent plans (01–04) will turn GREEN, and captures the bench baseline that plan 05 compares against for SC5.

Purpose: Pitfall #3 mandates test mocks migrate from `parse_event` shape to `parse_events` shape BEFORE shipping production change #2 — otherwise existing 322 tests break silently. Bench baseline must be captured pre-change.
Output: New test files (RED), extended conftest fixture, bench/ scripts, baseline.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09.1-demo-analysis-perf-pass/09.1-CONTEXT.md
@.planning/phases/09.1-demo-analysis-perf-pass/09.1-RESEARCH.md
@.planning/phases/09.1-demo-analysis-perf-pass/09.1-VALIDATION.md
@CLAUDE.md
@tests/conftest.py
@tests/test_ddm_analyzer_core.py
@tests/test_t0_detector_find_t0.py

<interfaces>
From RESEARCH.md §Verified APIs:
  parse_events(event_name: list[str]) -> List[Tuple[str, pandas.DataFrame]]
  parse_ticks(wanted_props, *, ticks: list[int] | range = None, players=None, prop_states=None)

From RESEARCH.md §Pattern 3 + D-04:
  find_t0(self, all_ticks_df, player_steamid, enemy_steamid,
          search_start_tick, search_end_tick,
          active_smokes=None, flash_intervals=None,
          ticks_by_sid: Optional[dict] = None) -> Tuple[Optional[int], str]

Empty-anchors guard (Pitfall #1): `if PARSE_TICKS_SELECTIVE and len(window) > 0` — empty list MUST NOT pass `ticks=[]`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend fake_parser fixture for parse_events shape (Pitfall #3 mitigation)</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (current fake_parser fixture shape)
    - .planning/phases/09.1-demo-analysis-perf-pass/09.1-RESEARCH.md §Pitfall 3 + §Example 3
    - .planning/phases/09.1-demo-analysis-perf-pass/09.1-CONTEXT.md (D-03 TDD discipline)
  </read_first>
  <behavior>
    - fake_parser must expose a `parse_events` attribute callable returning a list-of-tuples by default
    - Tests can override via `fake_parser.parse_events = lambda events: [...]` or `fake_parser.parse_events.return_value = [...]`
    - Existing `parse_event` (singular) attribute remains for backward compat with current 322 tests
  </behavior>
  <action>
    Inspect the current `fake_parser` fixture in tests/conftest.py. Add a `parse_events` callable attribute that, when called with a list of event names, returns `[(name, pd.DataFrame()) for name in event_names]` by default. Use `unittest.mock.MagicMock` if fixture already uses it, otherwise add a SimpleNamespace-style attribute. Do NOT remove existing `parse_event` attribute. Add a brief docstring noting that production code uses `parse_events([list])` (returns list-of-tuples) and tests can override via `fake_parser.parse_events = <spy>` per RESEARCH.md §Example 3.
  </action>
  <verify>
    <automated>python -m pytest tests/ -x --override-ini="addopts=--strict-markers" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "parse_events" tests/conftest.py` returns at least one match
    - `python -m pytest tests/ --override-ini="addopts=--strict-markers" -x` still passes (no regression — only additive change)
    - `python -c "from tests.conftest import *"` imports cleanly
  </acceptance_criteria>
  <done>fake_parser fixture has parse_events attribute; existing tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED tests for SC1 + SC4 in test_t0_detector_find_t0.py</name>
  <files>tests/test_t0_detector_find_t0.py</files>
  <read_first>
    - tests/test_t0_detector_find_t0.py (existing patterns)
    - t0_detector.py (current find_t0 signature, _AABB_OFFSETS list at lines 45-55)
    - .planning/phases/09.1-demo-analysis-perf-pass/09.1-RESEARCH.md §Example 5 + §Pattern 4
    - .planning/phases/09.1-demo-analysis-perf-pass/09.1-CONTEXT.md (D-04 fallback contract)
  </read_first>
  <behavior>
    - Test `test_aabb_offsets_center_first`: assert `_AABB_OFFSETS[0] == (0.0, 0.0, 0.5)` — center mass first per SC1
    - Test `test_find_t0_uses_ticks_by_sid_when_provided`: cached dict short-circuits bool-filter (SC4)
    - Test `test_find_t0_fallback_when_ticks_by_sid_none`: backward-compat — ticks_by_sid=None preserves current behavior (SC4)
  </behavior>
  <action>
    Append three new test functions to tests/test_t0_detector_find_t0.py per the patterns in RESEARCH.md §Example 5. Tests MUST initially FAIL (RED) — `_AABB_OFFSETS[0]` is currently NOT center, and `find_t0` does NOT yet accept `ticks_by_sid` kwarg. Use the exact test names `test_aabb_offsets_center_first`, `test_find_t0_uses_ticks_by_sid_when_provided`, `test_find_t0_fallback_when_ticks_by_sid_none` so plan 05 verify can grep them. Import `_AABB_OFFSETS` from `t0_detector`. For the ticks_by_sid tests, mock VisibilityChecker so they don't need .tri files (follow existing test_t0_detector_find_t0.py patterns).
  </action>
  <verify>
    <automated>python -m pytest tests/test_t0_detector_find_t0.py -k "center_first or ticks_by_sid" -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_aabb_offsets_center_first\|def test_find_t0_uses_ticks_by_sid_when_provided\|def test_find_t0_fallback_when_ticks_by_sid_none" tests/test_t0_detector_find_t0.py` returns 3
    - `python -m pytest tests/test_t0_detector_find_t0.py -k "center_first or ticks_by_sid" -x` FAILS (RED — production not yet changed). At least one of the new tests must fail; if all pass, the test is wrong.
    - Other tests in the file still pass
  </acceptance_criteria>
  <done>3 new RED tests added; they fail; rest of file's tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write RED tests for SC2 + SC3 + SC4 (analyzer side) in test_ddm_analyzer_core.py</name>
  <files>tests/test_ddm_analyzer_core.py</files>
  <read_first>
    - tests/test_ddm_analyzer_core.py (existing patterns + fixtures used)
    - tests/conftest.py (the now-extended fake_parser)
    - ddm_analyzer.py:733-806 (current parse_ticks + 7× parse_event call sites)
    - .planning/phases/09.1-demo-analysis-perf-pass/09.1-RESEARCH.md §Examples 3 + 4 + §Pitfalls 1, 2, 3
    - .planning/phases/09.1-demo-analysis-perf-pass/09.1-CONTEXT.md (D-02 fallback flag, D-04 cache contract)
  </read_first>
  <behavior>
    - SC2: `test_analyze_demo_uses_parse_events_batch` — analyze_demo calls `parse_events` ONCE with list including {player_hurt, player_death, weapon_fire, round_start}; calls to legacy `parse_event` fail the test
    - SC3: `test_parse_ticks_selective_uses_ticks_kwarg` — when PARSE_TICKS_SELECTIVE=True and hurt anchors exist, parse_ticks called with `ticks=` kwarg containing the anchor tick + before/after window
    - SC3: `test_parse_ticks_selective_disabled_full_parse` — with PARSE_TICKS_SELECTIVE=False, no `ticks=` kwarg passed
    - SC3: `test_parse_ticks_empty_anchors_falls_back_full` — Pitfall #1 guard: empty hurt list does NOT pass `ticks=[]` (else silent full-parse — must take the explicit fallback branch)
    - SC4: `test_analyze_engagement_passes_ticks_by_sid` — analyze_engagement_episode receives `ticks_by_sid` kwarg from analyze_demo
  </behavior>
  <action>
    Append five new test functions to tests/test_ddm_analyzer_core.py using the patterns from RESEARCH.md §Example 3 (SC2 parse_events spy) and §Example 4 (three SC3 selective-ticks tests). For SC4, add a test that monkeypatches `analyze_engagement_episode` and asserts the captured kwargs include a non-None `ticks_by_sid` dict. Use `monkeypatch.setattr("config.PARSE_TICKS_SELECTIVE", True/False)` for the flag toggling. Use the exact function names `test_analyze_demo_uses_parse_events_batch`, `test_parse_ticks_selective_uses_ticks_kwarg`, `test_parse_ticks_selective_disabled_full_parse`, `test_parse_ticks_empty_anchors_falls_back_full`, `test_analyze_engagement_passes_ticks_by_sid` for grep traceability. Tests MUST FAIL (RED) — production not yet changed.
  </action>
  <verify>
    <automated>python -m pytest tests/test_ddm_analyzer_core.py -k "parse_events_batch or parse_ticks_selective or empty_anchors or ticks_by_sid" -x 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_analyze_demo_uses_parse_events_batch\|def test_parse_ticks_selective_uses_ticks_kwarg\|def test_parse_ticks_selective_disabled_full_parse\|def test_parse_ticks_empty_anchors_falls_back_full\|def test_analyze_engagement_passes_ticks_by_sid" tests/test_ddm_analyzer_core.py` returns 5
    - The 5 new tests FAIL (RED) when run
    - Other tests in the file still pass (no regression introduced)
  </acceptance_criteria>
  <done>5 new RED tests added; they fail; rest of file's tests still green.</done>
</task>

<task type="auto">
  <name>Task 4: Build bench/run_bench.py + capture baseline.json</name>
  <files>bench/run_bench.py, bench/README.md, bench/baseline.json</files>
  <read_first>
    - .planning/phases/09.1-demo-analysis-perf-pass/09.1-CONTEXT.md (D-01 bench methodology)
    - .planning/phases/09.1-demo-analysis-perf-pass/09.1-RESEARCH.md §Example 1 (per-step bench harness)
    - ddm_analyzer.py:722-727 (existing analyze_demo profile=True hook)
    - for_analysis/ directory contents (verify ≥10 .dem files; if fewer, document in README and run with N available)
  </read_first>
  <action>
    Create `bench/` directory. Write `bench/run_bench.py` implementing D-01:
    - Selects up to 10 .dem files from `for_analysis/` (≥3 distinct maps preferred — read map_name from each demo's parse_header() to verify diversity; if not enough diversity, take first 10).
    - For EACH demo: spawn a fresh `subprocess.Popen([sys.executable, '-c', '<single-demo runner>'])` so .tri load is cold every run.
    - The single-demo runner instantiates DDMAnalyzer, calls `analyze_demo(profile=True, bulk_mode=True)`, prints JSON dict `{"demo": str, "step_seconds": {step: float}, "walltime_s": float, "n_moments": int}` to stdout.
    - Parent collects each line, aggregates into `bench_results.json` at provided `--output` path (default `bench/bench_results.json`).
    - CLI: `python bench/run_bench.py --output bench/baseline.json` (or `bench/post_change.json`).
    - Compare mode: `python bench/run_bench.py --compare bench/baseline.json bench/post_change.json` prints walltime ratio per demo + aggregate mean speedup.
    Write `bench/README.md` (≤25 lines) documenting: how to capture baseline, how to capture post-change, how to compare, expected speedup gate (≥3× per SC5), psutil optional.
    THEN run `python bench/run_bench.py --output bench/baseline.json` to capture the baseline. If <10 demos exist or any demo crashes, document in README + commit whatever baseline could be produced (note in commit message).
    Note: this task does NOT touch production code — only the bench harness and a one-time baseline capture.
  </action>
  <verify>
    <automated>python bench/run_bench.py --output bench/baseline.json && python -c "import json; d=json.load(open('bench/baseline.json')); assert 'walltime_s' in str(d) or len(d)>0, 'baseline empty'; print('baseline OK:', len(d) if isinstance(d,list) else 1, 'records')"</automated>
  </verify>
  <acceptance_criteria>
    - File `bench/run_bench.py` exists; `python bench/run_bench.py --help` exits 0
    - File `bench/README.md` exists with usage instructions
    - File `bench/baseline.json` exists; contains at least one record with `walltime_s` key
    - `grep -c "subprocess" bench/run_bench.py` ≥ 1 (cold cache via subprocess per D-01)
    - `grep -c "perf_counter" bench/run_bench.py` ≥ 1 (per-step timer markers per D-01)
  </acceptance_criteria>
  <done>bench harness functional; baseline.json captured pre-change; README explains usage.</done>
</task>

</tasks>

<verification>
After Wave 0:
- `python -m pytest tests/ --override-ini="addopts=--strict-markers"` — total count = (existing 322) + 8 new RED tests, with exactly the 8 new ones failing
- `bench/baseline.json` exists, non-empty
- `grep -rn "parse_events" tests/conftest.py` ≥ 1 hit
</verification>

<success_criteria>
8 new RED tests in test files; fake_parser extended; bench harness operational; baseline captured. Production code unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-demo-analysis-perf-pass/09.1-00-SUMMARY.md`
</output>
