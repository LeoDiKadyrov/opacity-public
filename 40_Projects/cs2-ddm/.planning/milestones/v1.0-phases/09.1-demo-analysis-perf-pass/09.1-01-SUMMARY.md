---
phase: "09.1"
plan: "01"
subsystem: "t0-detector"
tags: [perf, aabb, ordering, sc1]
requires:
  - "09.1-00 (RED tests in tests/test_t0_detector_find_t0.py)"
provides:
  - "Center-first _AABB_OFFSETS ordering — SC1 GREEN"
affects:
  - "find_t0(): intra-tick ray check order (semantics unchanged)"
  - "_find_los_enemy(): same _AABB_OFFSETS iteration"
tech-stack:
  added: []
  patterns:
    - "Cheap-first ordering (D-03): most-likely-visible target checked first inside per-tick loop"
key-files:
  created: []
  modified:
    - t0_detector.py
decisions:
  - "Pure rearrangement of tuple list — no constants or logic touched"
  - "Center mass tuple moved from index 8 (last) to index 0 (first); other 8 corners keep relative order"
metrics:
  duration: "~5 min"
  completed: "2026-05-07"
  tasks: 1
  files_changed: 1
---

# Phase 09.1 Plan 01: Center-Ray First in `_AABB_OFFSETS` Summary

Reordered `_AABB_OFFSETS` in `t0_detector.py` so center mass `(0.0, 0.0, 0.5)` is at index 0 — the most-likely-visible target now exits the corner loop earliest. Pure perf microopt, semantics preserved (any-corner-visible still wins T0).

## What Shipped

| Change | File | Lines | Effect |
|-|-|-|-|
| Move `(0.0, 0.0, 0.5)` from index 8 → 0; add inline comment | `t0_detector.py` | 45-55 | SC1 RED → GREEN |

## Verification

- `python -m pytest --override-ini="addopts=--strict-markers" tests/test_t0_detector_find_t0.py::test_aabb_offsets_center_first -v` → **PASSED**
- Full suite: `python -m pytest --override-ini="addopts=--strict-markers" tests/` → **323 passed, 7 failed** (the 7 still-RED are SC2/SC3/SC4 tests for plans 09.1-02/03/04 not yet executed — expected)
- Manual: `python -c "from t0_detector import _AABB_OFFSETS; print(_AABB_OFFSETS[0]); print(len(_AABB_OFFSETS))"` → `(0.0, 0.0, 0.5)` and `9` (length preserved, no entries dropped)

## Deviations from Plan

None — plan executed exactly as written. Pytest invocation required `--override-ini="addopts=--strict-markers"` (per CLAUDE.md) because `pytest.ini` injects `--cov` flags incompatible with single-test runs; this is a project convention, not a deviation from the plan's intent.

## Acceptance Criteria

| Criterion | Status |
|-|-|
| `_AABB_OFFSETS[0] == (0.0, 0.0, 0.5)` | PASS |
| `len(_AABB_OFFSETS)` unchanged (9) | PASS |
| `test_aabb_offsets_center_first` GREEN | PASS |
| No new failures in `tests/test_t0_detector_find_t0.py` (excluding pre-existing Wave 0 RED) | PASS |

## Self-Check: PASSED

- File modified: `t0_detector.py` — verified via Edit tool success + sanity python one-liner
- Commit: see Git Log section below
