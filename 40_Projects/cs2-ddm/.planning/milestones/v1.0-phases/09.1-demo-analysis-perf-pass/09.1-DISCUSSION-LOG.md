---
phase: 9.1
date: 2026-05-07
mode: discuss (manual — gsd-sdk unavailable)
---

# Discussion Log: Phase 9.1 — Demo Analysis Perf Pass

## Areas Selected
User selected ALL 4 gray areas from initial multiSelect.

## Area 1: Benchmark methodology

**Q1 — Demo set?**
Options: donk1.dem only / Subset 5 demos (Recommended) / All 26 demos
**User: 10 demos** (Other — compromise between subset and full)

**Q2 — Metric?**
Options: Walltime / Per-step breakdown / Both
**User: Both** — walltime for SC5 verdict + per-step for diagnostic attribution

**Q3 — Cache state?**
Options: Cold (Recommended) / Warm
**User: Cold** — matches batch_runner production reality

## Area 2: Selective parse_ticks fallback

**Q1 — Fallback strategy?**
Options: Feature flag (Recommended) / Rollback only #3 / Block phase
**User: Feature flag** — `config.PARSE_TICKS_SELECTIVE`, production-safe toggle

**Q2 — Regression definition?**
Options: Walltime worse (Recommended) / Speedup < 2x / Tests fail only
**User: Walltime worse than baseline** (>0% regression)

## Area 3: Commit granularity

**Q1 — Commit strategy?**
Options: 4 atomic commits (Recommended) / Group cheap+risky / 1 phase commit
**User: 4 atomic commits** — one per change, easy revert

**Q2 — TDD?**
Options: TDD per change (Recommended) / Tests after, regression-only
**User: TDD per change** — matches existing 322-test TDD discipline

## Area 4: Per-steamid cache scope

**Q1 — Where does cache live?**
Options: analyze_demo() local dict (Recommended) / T0Detector instance / Module-level functools.cache
**User: analyze_demo() local dict** — scope-bound, no ProcessPool leakage risk

**Q2 — find_t0() signature change?**
Options: Optional kwarg + fallback (Recommended) / Required param + fix tests
**User: Optional kwarg + fallback** — zero-regression for existing mocks

## Deferred Ideas
- awpy batched raycasts (`is_visible_batch`) — research spike candidate
- Inter-process BVH cache sharing
- `find_visible_enemies_at_tick` per-tick cache

## Scope-Creep Redirects
None encountered.

## Claude's Discretion
- Order of execution: #1 → #2 → #3 → #4 (cheap wins first; if #3 regresses, feature flag retains #1/#2/#4 gains)
- Number of plans: ~4 (one per change) + final bench/verify plan — final structure decided by /gsd-plan-phase
