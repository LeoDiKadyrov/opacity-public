---
phase: 09.1
plan: 05
type: execute
wave: 3
depends_on: ["09.1-01", "09.1-02", "09.1-03", "09.1-04"]
files_modified:
  - .planning/phases/09.1-demo-analysis-perf-pass/09.1-05-SUMMARY.md
  - bench/post_change.json
autonomous: false
requirements: [SC5]

must_haves:
  truths:
    - "Full pytest suite (322 baseline + 8 new SC1-SC4 tests = 330+) passes"
    - "bench/post_change.json captured AFTER all 4 changes landed"
    - "Aggregate walltime speedup vs bench/baseline.json ≥ 3.0× (SC5 gate)"
    - "Per-step breakdown captured for diagnostic attribution per change (#1-#4)"
    - "If <3× regression: PARSE_TICKS_SELECTIVE=False fallback verified to recover baseline (D-02 safety)"
  artifacts:
    - path: "bench/post_change.json"
      provides: "Post-change cold-cache bench results across same demo set as baseline"
    - path: ".planning/phases/09.1-demo-analysis-perf-pass/09.1-05-SUMMARY.md"
      provides: "Phase verification artifact: speedup table per demo, per-step deltas, decision on PARSE_TICKS_SELECTIVE default"
      contains: "speedup"
  key_links:
    - from: "bench/run_bench.py --compare"
      to: "bench/baseline.json + bench/post_change.json"
      via: "ratio computation"
      pattern: "compare"
---

<objective>
SC5: Run the full test suite + the bench harness post-change, compare to baseline, and produce the phase SUMMARY documenting speedup and any decisions (e.g., PARSE_TICKS_SELECTIVE default flip if regression detected per D-02).

Purpose: Final phase gate. Catches per-change regressions via per-step diagnostic table; protects against false-positive aggregate gains.
Output: post_change.json + SUMMARY.md with speedup verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-demo-analysis-perf-pass/09.1-CONTEXT.md
@.planning/phases/09.1-demo-analysis-perf-pass/09.1-RESEARCH.md
@.planning/phases/09.1-demo-analysis-perf-pass/09.1-VALIDATION.md
@CLAUDE.md
@bench/run_bench.py
@bench/baseline.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full pytest suite — verify all 322+ tests green post-change</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - .planning/phases/09.1-demo-analysis-perf-pass/09.1-VALIDATION.md (sampling rate)
    - .planning/phases/09.1-demo-analysis-perf-pass/09.1-CONTEXT.md (acceptance criteria)
  </read_first>
  <action>
    Run the full pytest suite without coverage (faster, per CLAUDE.md): `python -m pytest --override-ini="addopts=--strict-markers"`. Capture the summary line (e.g., "330 passed in 12.4s"). If ANY test fails, halt and report — do NOT proceed to bench step. Compare total count against pre-Phase 9.1 baseline (322 tests) — expect at least 322 + 8 new = 330 (Wave 0 added 8 RED tests; plans 01-04 turned them GREEN).
  </action>
  <verify>
    <automated>python -m pytest --override-ini="addopts=--strict-markers" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pytest summary shows `passed` and 0 `failed`
    - Test count ≥ 330 (322 baseline + 8 new SC1-SC4 tests)
    - No `error` or `FAILED` lines in pytest output
  </acceptance_criteria>
  <done>Full suite green; phase change set is test-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Capture post-change bench + compute speedup vs baseline</name>
  <files>bench/post_change.json</files>
  <read_first>
    - bench/run_bench.py (capture/compare CLI)
    - bench/baseline.json (pre-change reference)
    - .planning/phases/09.1-demo-analysis-perf-pass/09.1-CONTEXT.md (D-01 methodology, D-02 fallback rule)
  </read_first>
  <action>
    1. Run: `python bench/run_bench.py --output bench/post_change.json` — captures post-change walltime + per-step times for the SAME 10 demos as baseline. Cold cache (subprocess per demo).
    2. Run compare: `python bench/run_bench.py --compare bench/baseline.json bench/post_change.json` — prints per-demo walltime ratio + aggregate mean speedup + per-step ratios for {parse_events, parse_ticks, sid_cache, engagement_loop, walltime_total}.
    3. Capture stdout of the compare run to be embedded in SUMMARY.md (next task).
    4. If aggregate walltime speedup < 3.0×: do NOT auto-flip PARSE_TICKS_SELECTIVE. Surface the regression in SUMMARY.md with per-step diagnostic attribution and stop — checkpoint task 3 will gate the decision.
  </action>
  <verify>
    <automated>python bench/run_bench.py --compare bench/baseline.json bench/post_change.json 2>&1 | tee bench/_compare_out.txt | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `bench/post_change.json` exists; same demo set as baseline
    - `python bench/run_bench.py --compare bench/baseline.json bench/post_change.json` prints aggregate speedup
    - Per-step breakdown present for each demo (parse_events, parse_ticks, sid_cache, engagement_loop)
  </acceptance_criteria>
  <done>post_change.json captured; speedup computed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verifies bench results meet SC5 gate (≥3× speedup)</name>
  <what-built>
    All 4 perf changes landed (#1 center-ray, #2 parse_events batch, #3 selective parse_ticks, #4 sid cache). Full pytest suite green (≥330 tests). bench/post_change.json captured with cold cache across 10 demos.
  </what-built>
  <how-to-verify>
    1. Open `bench/_compare_out.txt` (or re-run `python bench/run_bench.py --compare bench/baseline.json bench/post_change.json`).
    2. Confirm aggregate walltime mean speedup ≥ 3.0× across the 10-demo set (SC5 gate).
    3. Sanity-check per-step ratios:
       - `parse_events` step should show ~3-5× speedup (SC2)
       - `parse_ticks` step should show ~3-5× speedup (SC3)
       - `engagement_loop` step should show ~3× speedup (SC4)
       - `find_t0` inner cost roughly flat (SC1 is cosmetic ordering)
    4. If aggregate < 3.0×: identify which step regressed. If `parse_ticks` step is worse than baseline (>0% regression per D-02 definition), respond `flip-flag` to set `config.PARSE_TICKS_SELECTIVE = False` and re-run bench. If a different step regressed, respond with diagnosis and we'll plan a fix.
    5. If all metrics look good: respond `approved`.
  </how-to-verify>
  <resume-signal>Type "approved" / "flip-flag" / describe issue</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Write 09.1-05-SUMMARY.md with speedup table + decisions</name>
  <files>.planning/phases/09.1-demo-analysis-perf-pass/09.1-05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/09.1-demo-analysis-perf-pass/09.1-CONTEXT.md (acceptance criteria)
    - bench/_compare_out.txt (compare stdout from task 2)
    - bench/baseline.json + bench/post_change.json
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY template)
  </read_first>
  <action>
    Write the phase SUMMARY following the SUMMARY template. Sections to include:

    - **Phase verdict:** SC1-SC5 each marked PASS / FAIL with one-line evidence (test name + bench number).
    - **Speedup table:** markdown table with minimum separator (`|-|-|`) — columns: Demo | Walltime baseline (s) | Walltime post (s) | Ratio. One row per demo + an aggregate Mean row.
    - **Per-step attribution table:** Step | Baseline mean | Post mean | Ratio. Steps: parse_events, parse_ticks, sid_cache, engagement_loop, walltime_total.
    - **Decisions:** PARSE_TICKS_SELECTIVE default (True unless flipped per D-02), any tuned window constants if changed during verify.
    - **Files changed:** list of files modified across plans 01-04 + Wave 0.
    - **Test count:** before/after.
    - **Open questions / follow-ups:** any deferred items surfaced (e.g., awpy is_visible_batch still deferred).

    Append entry to ROADMAP.md Phase 9.1 plans list (mark `[x]` for plans 00-05) — mirror the convention used in the existing Phase 9 roadmap entries.

    Update STATE.md "Decisions" section with one-line entries for any non-trivial discretion choices made during execution (e.g., final values of `_SELECTIVE_WINDOW_BEFORE_TICKS` / `_AFTER_TICKS` if tuned, PARSE_TICKS_SELECTIVE default if flipped).
  </action>
  <verify>
    <automated>test -f .planning/phases/09.1-demo-analysis-perf-pass/09.1-05-SUMMARY.md && grep -c "Speedup\|speedup\|SC5" .planning/phases/09.1-demo-analysis-perf-pass/09.1-05-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the SUMMARY path
    - Contains a markdown table with `|-|-|` separator (per CLAUDE.md table rules)
    - Contains explicit PASS/FAIL verdict for each of SC1-SC5
    - References both baseline.json and post_change.json paths
    - ROADMAP.md Phase 9.1 entry now shows `[x]` for plans 00-05
  </acceptance_criteria>
  <done>Phase SUMMARY written; ROADMAP + STATE updated; phase ready for `/gsd-verify-work`.</done>
</task>

</tasks>

<verification>
- All 5 SC verified in SUMMARY.md
- ROADMAP.md Phase 9.1 entry marked complete
- STATE.md decisions updated
- post_change.json + baseline.json both exist for future bench reruns
</verification>

<success_criteria>
SC5 PASS: full suite green AND ≥3× walltime speedup (or flag-off recovery documented per D-02). Phase 9.1 verdict committed in SUMMARY.md.
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-demo-analysis-perf-pass/09.1-05-SUMMARY.md` (this IS the output of this plan). Commit message: `docs(09.1): phase verification + bench results (SC5)`.
</output>
