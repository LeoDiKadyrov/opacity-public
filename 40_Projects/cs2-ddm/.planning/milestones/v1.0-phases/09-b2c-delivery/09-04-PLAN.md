---
phase: 09-b2c-delivery
plan: 04
type: execute
wave: 4
depends_on:
  - 09-03-PLAN.md
files_modified: []
autonomous: false
requirements:
  - Djok delivery format for paying users
  - shareable report output

must_haves:
  truths:
    - "Operator can click Download Report in Streamlit Interpretation tab"
    - "Downloaded HTML file opens in browser with no network access"
    - "Worst metric card appears at top of Interpretation section with gold border"
    - "All three sections present in report: Interpretation, Distributions, Raw Data"
    - "Zero external URLs in downloaded HTML (safety gate)"
  artifacts:
    - path: ".planning/phases/09-b2c-delivery/09-VERIFICATION.md"
      provides: "SC3/SC4/SC5 verification results"
  key_links:
    - from: "Streamlit Download Report button"
      to: "djok_report_{steamid}.html"
      via: "st.download_button data=bytes"
      pattern: "Download Report"
---

<objective>
Manual verification checkpoint — operator runs Streamlit, clicks Download Report,
opens HTML file in browser offline, and confirms SC3/SC4/SC5 visually.

Purpose: Automated tests cover structure and safety gate, but visual confirmation of
brand colors, typography, and the worst metric card layout requires human eyes.

Output: VERIFICATION.md with SC results and operator sign-off.

Note: SC1 (public deploy) and SC2 (FACEIT URL auto-download) are DEFERRED to Phase 10
per D-01/D-02. This checkpoint verifies SC3, SC4, SC5 only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-b2c-delivery/09-CONTEXT.md
@.planning/phases/09-b2c-delivery/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and safety gate check</name>
  <files></files>
  <read_first>
    - (none — automated checks only)
  </read_first>
  <action>
    Run the following automated checks before the manual checkpoint:

    1. Full test suite:
       ```
       python -m pytest tests/ --override-ini="addopts=--strict-markers" -v
       ```
       Expected: all tests pass (300+ total). No failures.

    2. External URL safety gate:
       ```python
       python -c "
       import report_generator, re
       html = report_generator.generate_html_report(
           player_steamid=76561198386265483,
           benchmark_steamid=76561198386265483,
           benchmark_name='donk',
       ).decode()
       external = re.findall(r'(?:href|src)=[\"\'](https?://[^\"\']+)', html)
       if external:
           print('FAIL:', external)
       else:
           print('PASS — zero external URLs')
       with open('djok_report_sample.html', 'w', encoding='utf-8') as f:
           f.write(html)
       print('Sample report written to djok_report_sample.html')
       "
       ```

    3. Write check results summary to screen. If any automated check fails, fix before
       proceeding to manual checkpoint.
  </action>
  <verify>
    <automated>python -m pytest tests/ --override-ini="addopts=--strict-markers" -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - All tests pass
    - Safety gate returns "PASS — zero external URLs"
    - djok_report_sample.html written to project root
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete HTML report generated by report_generator.py, integrated into Streamlit
    as a Download Report button in the Interpretation tab. djok_report_sample.html
    written to project root by Task 1 for offline inspection.
  </what-built>
  <how-to-verify>
    **Step 1 — Offline HTML inspection (no network needed):**
    Open djok_report_sample.html in your browser (double-click or File > Open).
    Disconnect from internet first, or use browser DevTools Network tab to confirm
    zero network requests fire on load.

    Verify visually:
    - [ ] SC5: Page opens with a "Your biggest opportunity" card at the top of Interpretation section — gold (#e8b84b) border, displays worst metric name + tier + drill text
    - [ ] SC3 visual: "Interpretation", "Distributions", "Raw Data" section headers present
    - [ ] Distributions section has at least one chart (histogram with colored bars)
    - [ ] Raw Data section has a table with engagement rows
    - [ ] Background is dark (#0e0e12), accent is gold (#e8b84b) — consistent Djok brand
    - [ ] No broken image icons, no "Font not found" errors
    - [ ] Numbers in mono font (JetBrains Mono fallback to Consolas)

    **Step 2 — Streamlit live test (SC3 download flow):**
    ```
    streamlit run app.py
    ```
    1. Enter donk SteamID (76561198386265483) in sidebar Configuration section
    2. Navigate to Interpretation section (scroll down)
    3. Confirm "Download Report" button appears below the Peek/Hold tabs
    4. Click "Download Report" — browser should offer djok_report_{steamid}.html download
    5. Open downloaded file — confirm same visual as Step 1

    **SC4 check (email gate already live):**
    SC4 = Tally form on landing page collects email + SteamID. This is already live
    from prior phases. No new verification needed here — note as PASS (pre-existing).

    **Deferred (out of scope for Phase 9):**
    - SC1: public deploy — Phase 10
    - SC2: FACEIT URL auto-download — Phase 10 (API approval pending)
  </how-to-verify>
  <resume-signal>
    Report "approved" with SC results: SC3 PASS/FAIL, SC4 PASS (pre-existing), SC5 PASS/FAIL.
    If FAIL on any SC, describe what's wrong (wrong color, missing section, etc.) and Claude will fix.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| downloaded HTML → browser | Static file; no JS execution risk; no external fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | Information Disclosure | djok_report_sample.html written to project root | accept | Local operator machine only; file is not committed to git (add to .gitignore if needed) |
</threat_model>

<verification>
Manual: operator confirms SC3 and SC5 in browser.
Automated (pre-checkpoint):
  python -m pytest tests/ --override-ini="addopts=--strict-markers" -q
  python -c "import report_generator,re; html=report_generator.generate_html_report(76561198386265483,76561198386265483,'donk').decode(); assert not re.findall(r'(?:href|src)=[\"\'](https?://)', html); print('PASS')"
</verification>

<success_criteria>
- SC3: HTML report downloads from Streamlit and opens in browser with all three sections (PASS)
- SC4: Email gate via Tally form — already live, confirmed PASS (pre-existing)
- SC5: Worst metric card at top of Interpretation section with gold border (PASS)
- SC1/SC2: DEFERRED to Phase 10 (noted in VERIFICATION.md)
- VERIFICATION.md written to .planning/phases/09-b2c-delivery/
</success_criteria>

<output>
After completion, create `.planning/phases/09-b2c-delivery/09-04-SUMMARY.md`
Also create `.planning/phases/09-b2c-delivery/09-VERIFICATION.md` with SC results.
Update ROADMAP.md Phase 9 status.
</output>
