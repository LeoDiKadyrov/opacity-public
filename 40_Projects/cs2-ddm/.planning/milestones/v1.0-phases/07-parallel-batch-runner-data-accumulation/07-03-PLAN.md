---
phase: 07-parallel-batch-runner-data-accumulation
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - ddm_analyzer.py
autonomous: true
requirements:
  - "Phase 7 D-07: demo_name column populated in engagements rows"

must_haves:
  truths:
    - "DDMAnalyzer принимает demo_name параметр в __init__"
    - "self.demo_name сохраняется как атрибут"
    - "demo_name инжектируется в results_df перед вызовом save_to_db"
    - "Если demo_name не передан — использует Path(demo_path).stem как fallback"
    - "Все существующие 256 тестов остаются зелёными"
  artifacts:
    - path: "ddm_analyzer.py"
      provides: "demo_name parameter in __init__"
      contains: "demo_name"
  key_links:
    - from: "DDMAnalyzer.__init__"
      to: "self.demo_name"
      via: "Path(demo_path).stem fallback"
      pattern: "self.demo_name"
---

<objective>
Добавить параметр `demo_name: str = ""` в DDMAnalyzer.__init__ и прокинуть его в results_df
перед вызовом save_to_db внутри analyze_demo(). Это позволяет batch worker передавать demo_name
при создании анализатора — чище, чем UPDATE после сохранения.

Purpose: Строки в engagements.demo_name будут заполнены корректно при batch run без отдельного
UPDATE запроса в каждом worker. Plan 07-02 делает UPDATE как fallback, но если DDMAnalyzer
сам инжектирует demo_name — UPDATE не нужен (но не вредит).

Output: DDMAnalyzer с demo_name параметром. Все 256+ тестов зелёные.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-parallel-batch-runner-data-accumulation/07-CONTEXT.md

<interfaces>
<!-- DDMAnalyzer.__init__ текущая сигнатура (ddm_analyzer.py lines 50-75): -->
```python
def __init__(
    self,
    demo_path: str,
    player_steamid: int,
    match_id=None,
    tickrate: int = 64,
    debug_prints: bool = False,
    enemy_velocity_threshold: float = ENEMY_VELOCITY_HOLD_THRESHOLD_UPS,
    player_velocity_threshold: float = VELOCITY_PEEK_THRESHOLD_UPS,
):
    try:
        self.parser = DemoParser(demo_path)
    except Exception as e:
        print(f"Error initializing DemoParser: {e}")
        raise

    self.demo_path = demo_path
    self.player_steamid = player_steamid
    self.tickrate = tickrate
    self.debug_prints = debug_prints
    self.match_id = match_id if match_id is not None else self._hash_demo(demo_path)
    self.logger = get_logger(self.match_id, debug_prints)
    self.analysis_moments: List[AnalysisMoment] = []
    self.enemy_velocity_threshold = enemy_velocity_threshold
    self.player_velocity_threshold = player_velocity_threshold
    self.last_accepted_t2_tick: Optional[int] = None  # overlapping window gate (D-07)
    ...
```

<!-- app.py вызов DDMAnalyzer (lines 166-172) — существующий call без demo_name, должен работать: -->
```python
analyzer = DDMAnalyzer(
    demo_path, player_steamid,
    match_id=match_label, tickrate=tickrate, debug_prints=False,
    enemy_velocity_threshold=enemy_vel,
    player_velocity_threshold=player_vel,
)
```

<!-- batch_runner.py вызов (из Plan 07-02) — будет передавать demo_name: -->
```python
analyzer = DDMAnalyzer(
    demo_path=demo_path,
    player_steamid=player_steamid,
    match_id=match_id,
    tickrate=tickrate,
    debug_prints=False,
)
# После этого плана можно добавить:
# demo_name=demo_name,
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить demo_name параметр в DDMAnalyzer и прокинуть в results_df</name>
  <files>ddm_analyzer.py</files>

  <read_first>
    - ddm_analyzer.py строки 50-120 (полный __init__ и начало analyze_demo)
    - ddm_analyzer.py (grep для save_to_db — найти все вызовы db_utils.save_to_db внутри файла)
  </read_first>

  <action>
Минимальные изменения в ddm_analyzer.py в двух местах:

**Место 1: __init__ параметр (добавить `demo_name` после `player_velocity_threshold`)**

Текущая сигнатура заканчивается на:
```python
    player_velocity_threshold: float = VELOCITY_PEEK_THRESHOLD_UPS,
):
```

Добавить параметр:
```python
    player_velocity_threshold: float = VELOCITY_PEEK_THRESHOLD_UPS,
    demo_name: str = "",
):
```

В теле __init__, после `self.player_velocity_threshold = player_velocity_threshold`, добавить:
```python
    # demo_name: human-readable label for DB queries and Streamlit dropdowns.
    # Defaults to demo filename stem if not provided.
    self.demo_name: str = demo_name if demo_name else Path(self.demo_path).stem
```

Добавить `from pathlib import Path` в импорты если не присутствует (проверить — ddm_analyzer.py
уже импортирует `import os`, Path может отсутствовать; использовать `os.path.splitext(os.path.basename(self.demo_path))[0]` как альтернативу если Path не импортирован).

**Место 2: analyze_demo — инжекция demo_name в results_df перед save_to_db**

Найти в analyze_demo() место где вызывается `db_utils.save_to_db(...)` для engagements.
ПЕРЕД этим вызовом добавить:
```python
        if not results_df.empty:
            results_df["demo_name"] = self.demo_name
```

Важно: добавить именно перед save_to_db вызовом, не после.

**Что НЕ менять:**
- Никаких изменений в логике analyze_demo, auto_build_moments, T0/T1/T2 detection
- Никаких изменений в __init__ кроме двух добавок выше
- Не изменять существующие параметры или их порядок (не сломать app.py)
- Не изменять CSV output логику (batch не использует CSV, но single-demo режим должен работать)

**Backward compatibility**: `demo_name=""` default → старые вызовы без параметра (app.py, run_analysis.py, все тесты) продолжают работать без изменений.
  </action>

  <verify>
    <automated>python -m pytest tests/ -x -q --override-ini="addopts=--strict-markers" 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "demo_name" ddm_analyzer.py` >= 3 (параметр, self.demo_name, инжекция в df)
    - `python -c "from ddm_analyzer import DDMAnalyzer; import inspect; sig = inspect.signature(DDMAnalyzer.__init__); assert 'demo_name' in sig.parameters; print('demo_name param OK')"` exits 0
    - `python -m pytest tests/ -x -q --override-ini="addopts=--strict-markers"` exits 0 (все 256+ тестов)
    - app.py вызов DDMAnalyzer без demo_name по-прежнему работает (fallback к Path(demo_path).stem)
  </acceptance_criteria>

  <done>
    DDMAnalyzer принимает demo_name параметр (default=""). self.demo_name = demo_name or stem.
    demo_name инжектируется в results_df перед save_to_db. Все 256+ тестов зелёные.
    app.py и run_analysis.py работают без изменений (backward compatible).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → DDMAnalyzer | demo_name передаётся из batch_runner; используется только для записи в DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Tampering | demo_name → results_df["demo_name"] → save_to_db | accept | demo_name = Path(demo_path).stem — берётся из filesystem path, не из user text input. SQL запись через pandas to_sql с parameterized bindings |
</threat_model>

<verification>
```bash
# Параметр добавлен
python -c "
import inspect
from ddm_analyzer import DDMAnalyzer
sig = inspect.signature(DDMAnalyzer.__init__)
assert 'demo_name' in sig.parameters, 'demo_name param missing'
param = sig.parameters['demo_name']
assert param.default == '', f'Expected default empty string, got: {param.default}'
print('demo_name parameter OK, default=\"\"')
"

# Регрессия — все тесты
python -m pytest tests/ -x -q --override-ini="addopts=--strict-markers"
```
</verification>

<success_criteria>
1. `DDMAnalyzer.__init__` принимает `demo_name: str = ""` как keyword argument
2. `self.demo_name` устанавливается как переданное значение или `Path(demo_path).stem` если пустое
3. `results_df["demo_name"]` = `self.demo_name` перед вызовом `save_to_db`
4. Все 256 существующих тестов остаются зелёными (backward compatible изменение)
5. `DDMAnalyzer(demo_path, steamid)` без demo_name — по-прежнему работает
</success_criteria>

<output>
После завершения создать `.planning/phases/07-parallel-batch-runner-data-accumulation/07-03-SUMMARY.md`
</output>
