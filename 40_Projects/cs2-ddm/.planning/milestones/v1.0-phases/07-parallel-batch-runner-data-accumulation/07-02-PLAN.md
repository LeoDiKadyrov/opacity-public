---
phase: 07-parallel-batch-runner-data-accumulation
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - batch_runner.py
autonomous: true
requirements:
  - "Phase 7 SC1: batch processes all demos без manual intervention"
  - "Phase 7 SC3: runtime < 60 min (параллельный ProcessPoolExecutor)"
  - "Phase 7 D-08: ProcessPoolExecutor, CPU-bound workers"
  - "Phase 7 D-09: log-and-continue, batch_errors.log"
  - "Phase 7 D-11: skip if already processed"

must_haves:
  truths:
    - "analyze_demo_worker — top-level функция в batch_runner.py (не в классе, не в app.py)"
    - "Worker создаёт DDMAnalyzer ВНУТРИ своего процесса (DDMAnalyzer не picklable)"
    - "Все аргументы worker — picklable примитивы (str, int)"
    - "Worker возвращает dict {status, match_id, demo, engagements, attempts} или {status: error, traceback}"
    - "BatchRunner.run() pre-assigns все match_ids в main thread перед запуском pool"
    - "is_processed() check пропускает уже обработанные демо (D-11)"
    - "mark_processed() вызывается после успешного сохранения (D-10)"
    - "Ошибки логируются в batch_errors.log с traceback"
    - "BatchRunner.scan_demos() возвращает список .dem файлов из for_analysis/"
  artifacts:
    - path: "batch_runner.py"
      provides: "analyze_demo_worker top-level function, BatchRunner class"
      contains: "def analyze_demo_worker"
  key_links:
    - from: "BatchRunner.run()"
      to: "analyze_demo_worker"
      via: "ProcessPoolExecutor.submit()"
      pattern: "ProcessPoolExecutor"
    - from: "analyze_demo_worker"
      to: "db_utils.mark_processed()"
      via: "после успешного analyze_demo()"
      pattern: "mark_processed"
---

<objective>
Создать batch_runner.py — новый модуль с top-level worker функцией для ProcessPoolExecutor и
классом BatchRunner. Это ключевой модуль Phase 7: параллельная обработка, idempotency check,
error isolation, pre-assigned match_ids.

Purpose: Worker ОБЯЗАН быть в отдельном импортируемом модуле — Windows spawn start method
требует это. Класс BatchRunner инкапсулирует логику scan + pre-assign + pool execution.

Output: batch_runner.py с analyze_demo_worker (picklable top-level) и BatchRunner class.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-parallel-batch-runner-data-accumulation/07-CONTEXT.md
@.planning/phases/07-parallel-batch-runner-data-accumulation/07-RESEARCH.md
@.planning/phases/07-parallel-batch-runner-data-accumulation/07-PATTERNS.md

<interfaces>
<!-- DDMAnalyzer.__init__ signature (из ddm_analyzer.py lines 50-58): -->
```python
class DDMAnalyzer:
    def __init__(
        self,
        demo_path: str,
        player_steamid: int,
        match_id=None,          # int — pre-assigned
        tickrate: int = 64,
        debug_prints: bool = False,
        enemy_velocity_threshold: float = ENEMY_VELOCITY_HOLD_THRESHOLD_UPS,  # default ok
        player_velocity_threshold: float = VELOCITY_PEEK_THRESHOLD_UPS,       # default ok
    ): ...

    def analyze_demo(self, bulk_mode: bool = False, attempts_mode: bool = False):
        """Returns (results_df: pd.DataFrame, attempts: List[DuelAttempt]).
        IMPORTANT: вызывает db_utils.save_to_db(engagements) ВНУТРИ — worker НЕ должен
        повторно вызывать save_to_db для engagements."""
```

<!-- DuelAttempt dataclass (из duel_attempts.py): -->
```python
@dataclass
class DuelAttempt:
    match_id: str
    map_name: str
    t0_tick: int
    enemy_steamid: int
    was_killed: bool
    bullets_fired: int
    bullets_hit: int
    engagement_type: str
    player_velocity_ups: float
    crosshair_angle_deg: float
    hurt_victims_in_window: str = ""
    fires_in_cluster: int = 0
    player_steamid: Optional[int] = None
```

<!-- db_utils.py новые функции (из Plan 07-01): -->
```python
def init_db(db_path: str) -> None: ...
def is_processed(db_path: str, demo_filename: str, player_steamid: int) -> bool: ...
def mark_processed(db_path: str, demo_filename: str, player_steamid: int, match_id: int) -> None: ...
def get_next_match_id(db_path: str) -> int: ...
def save_to_db(df: pd.DataFrame, db_path: str, table: str, match_id: Union[int, str]) -> None: ...
```

<!-- config.py константы (из Plan 07-01): -->
```python
DEFAULT_BATCH_WORKERS: int = 8
BATCH_INPUT_DIR: str = "for_analysis"
BATCH_ERRORS_LOG: str = "batch_errors.log"
DB_PATH: str = "analytics.db"
```

<!-- run_analysis.py — шаблон для worker body (lines 39-66): -->
```python
for match_id, filename in DEMOS:
    demo_path = rf"{BASE}\{filename}"
    try:
        analyzer = DDMAnalyzer(demo_path, PLAYER_STEAMID, match_id=match_id, tickrate=64)
    except Exception as e:
        print(f"  ERROR initialising analyzer: {e}"); continue
    try:
        results_df, _ = analyzer.analyze_demo(bulk_mode=True)
    except Exception as e:
        print(f"  ERROR during analysis: {e}"); continue
    # db_utils.save_to_db уже вызван внутри analyze_demo() — не дублировать
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать batch_runner.py — analyze_demo_worker + BatchRunner</name>
  <files>batch_runner.py, tests/test_batch_runner.py</files>

  <read_first>
    - run_analysis.py (полностью — шаблон для worker body)
    - config.py (строки 1-20 и конец файла — импортировать DB_PATH, DEFAULT_BATCH_WORKERS, BATCH_INPUT_DIR, BATCH_ERRORS_LOG)
    - .planning/phases/07-parallel-batch-runner-data-accumulation/07-RESEARCH.md (Focus Area 3: Worker Function Design, Focus Area 4: match_id Assignment)
  </read_first>

  <behavior>
    - Test: analyze_demo_worker picklable — `import pickle; pickle.dumps(analyze_demo_worker)` не бросает
    - Test: analyze_demo_worker с несуществующим demo_path возвращает dict с status="error" и ключом "traceback"
    - Test: analyze_demo_worker возвращает dict с ключами {status, match_id, demo, engagements, attempts} при status="ok"
    - Test: BatchRunner.scan_demos(dir_path) возвращает список Path объектов *.dem файлов (отсортированных)
    - Test: BatchRunner.scan_demos() на пустой директории возвращает []
    - Test: BatchRunner.filter_unprocessed(demo_paths, player_steamid, db_path) пропускает already-processed
    - Test: BatchRunner.filter_unprocessed() включает demos не в processed_matches
    - Test: pre_assign_match_ids() возвращает list[(match_id, demo_path)] длиной равной input
    - Test: pre_assign_match_ids() на пустой БД начинает с match_id=1
    - Test: pre_assign_match_ids() на БД с MAX=5 начинает с 6
  </behavior>

  <action>
Создать `batch_runner.py` в корне проекта. Полная структура модуля:

```python
"""
Parallel batch runner for DDM demo analysis.

Worker function is top-level (module-level) — required for Windows spawn
multiprocessing where child processes import this module directly.

Usage:
    from batch_runner import BatchRunner
    runner = BatchRunner(db_path="analytics.db", n_workers=8)
    results = runner.run(demo_paths, player_steamid=76561198386265483)
"""

from __future__ import annotations

import logging
import os
import sys
import traceback as _traceback
from pathlib import Path
from typing import Dict, List, Optional, Tuple

from config import (
    DB_PATH,
    DEFAULT_BATCH_WORKERS,
    BATCH_INPUT_DIR,
    BATCH_ERRORS_LOG,
)
import db_utils


# ── Module-level logger ──────────────────────────────────────────────────────

def _get_batch_logger() -> logging.Logger:
    logger = logging.getLogger("BatchRunner")
    if not logger.handlers:
        fh = logging.FileHandler(BATCH_ERRORS_LOG, encoding="utf-8")
        fh.setFormatter(logging.Formatter(
            "%(asctime)s [%(name)s] %(levelname)s: %(message)s"
        ))
        logger.addHandler(fh)
        sh = logging.StreamHandler(sys.stdout)
        sh.setFormatter(logging.Formatter("[%(name)s] %(message)s"))
        logger.addHandler(sh)
    logger.setLevel(logging.INFO)
    logger.propagate = False
    return logger


logger = _get_batch_logger()


# ── Top-level worker function (MUST stay module-level for Windows spawn) ─────

def analyze_demo_worker(args: Tuple[str, int, int, str, int]) -> Dict:
    """
    Worker callable for ProcessPoolExecutor.

    All arguments are plain picklable types (no DDMAnalyzer, no connection).
    DDMAnalyzer is instantiated INSIDE the worker process.

    Args:
        args: (demo_path, player_steamid, match_id, db_path, tickrate)

    Returns:
        dict with keys:
            status: "ok" | "error"
            match_id: int
            demo: str (demo stem name)
            engagements: int (0 on error)
            attempts: int (0 on error)
            error: str (only on error)
            traceback: str (only on error)
    """
    demo_path, player_steamid, match_id, db_path, tickrate = args

    # Imports inside worker — clean namespace after Windows spawn
    import sqlite3
    from ddm_analyzer import DDMAnalyzer
    import db_utils as _db
    import pandas as pd
    import dataclasses

    # Ensure WAL mode on first worker DB touch (idempotent if already WAL)
    try:
        import sqlite3 as _sq
        conn = _sq.connect(db_path, timeout=30)
        conn.execute("PRAGMA journal_mode=WAL")
        conn.execute("PRAGMA busy_timeout=10000")
        conn.close()
    except Exception:
        pass  # If DB not accessible, DDMAnalyzer will fail with a clear error

    demo_name = Path(demo_path).stem

    try:
        analyzer = DDMAnalyzer(
            demo_path=demo_path,
            player_steamid=player_steamid,
            match_id=match_id,
            tickrate=tickrate,
            debug_prints=False,
        )
        results_df, attempts = analyzer.analyze_demo(bulk_mode=True, attempts_mode=True)

        # Inject demo_name into results BEFORE save (analyze_demo saves engagements internally).
        # Because analyze_demo() already called save_to_db(), we need to UPDATE demo_name
        # in the DB for the rows just written.
        if not results_df.empty and match_id is not None:
            try:
                import sqlite3 as _sq2
                with _sq2.connect(db_path, timeout=30) as _c:
                    _c.execute(
                        "UPDATE engagements SET demo_name=? WHERE match_id=?",
                        (demo_name, str(match_id)),
                    )
            except Exception as _e:
                pass  # Non-fatal: demo_name update failure logged but doesn't block

        # Save duel_attempts to DB (analyze_demo does NOT save these)
        n_attempts = 0
        if attempts:
            n_attempts = len(attempts)
            att_df = pd.DataFrame([dataclasses.asdict(a) for a in attempts])
            att_df["demo_name"] = demo_name
            _db.save_to_db(att_df, db_path, "duel_attempts", match_id)

        # Mark as processed AFTER all writes succeed
        _db.mark_processed(
            db_path, os.path.basename(demo_path), player_steamid, match_id
        )

        return {
            "status": "ok",
            "match_id": match_id,
            "demo": demo_name,
            "engagements": len(results_df),
            "attempts": n_attempts,
        }

    except Exception as exc:
        tb = _traceback.format_exc()
        return {
            "status": "error",
            "match_id": match_id,
            "demo": demo_name,
            "engagements": 0,
            "attempts": 0,
            "error": str(exc),
            "traceback": tb,
        }


# ── BatchRunner class ─────────────────────────────────────────────────────────

class BatchRunner:
    """Orchestrates parallel batch processing of .dem files.

    Usage:
        runner = BatchRunner(db_path=DB_PATH, n_workers=DEFAULT_BATCH_WORKERS)
        results = runner.run(
            demo_paths=[Path("for_analysis/demo1.dem"), ...],
            player_steamid=76561198386265483,
            tickrate=64,
            force_reprocess=False,
            progress_callback=None,  # callable(done: int, total: int, current: str)
        )
    """

    def __init__(
        self,
        db_path: str = DB_PATH,
        n_workers: int = DEFAULT_BATCH_WORKERS,
    ) -> None:
        self.db_path = db_path
        self.n_workers = n_workers

    def scan_demos(self, input_dir: Optional[str] = None) -> List[Path]:
        """Return sorted list of .dem files in input_dir (default: BATCH_INPUT_DIR)."""
        d = Path(input_dir or BATCH_INPUT_DIR)
        d.mkdir(exist_ok=True)
        return sorted(d.glob("*.dem"))

    def filter_unprocessed(
        self,
        demo_paths: List[Path],
        player_steamid: int,
        force: bool = False,
    ) -> List[Path]:
        """Return demos not yet in processed_matches for this player.

        If force=True (D-12): deletes existing rows for already-processed demos
        via force_reprocess_demo() before returning the full list, so workers
        write fresh rows without duplicates.
        """
        if force:
            # D-12: delete old engagements/duel_attempts rows + processed_matches record
            for p in demo_paths:
                db_utils.force_reprocess_demo(self.db_path, p.name, player_steamid)
            return list(demo_paths)
        return [
            p for p in demo_paths
            if not db_utils.is_processed(self.db_path, p.name, player_steamid)
        ]

    def pre_assign_match_ids(
        self, demos_to_process: List[Path]
    ) -> List[Tuple[int, Path]]:
        """Assign sequential match_ids in main thread BEFORE spawning workers.

        Prevents race conditions from 8 concurrent SELECT MAX(match_id)+1 calls.
        Returns list of (match_id, demo_path) pairs.
        """
        if not demos_to_process:
            return []
        next_id = db_utils.get_next_match_id(self.db_path)
        return [(next_id + i, p) for i, p in enumerate(demos_to_process)]

    def run(
        self,
        demo_paths: List[Path],
        player_steamid: int,
        tickrate: int = 64,
        force_reprocess: bool = False,
        progress_callback: Optional[object] = None,
    ) -> List[Dict]:
        """Process demos in parallel. Returns list of result dicts.

        progress_callback(done: int, total: int, current: str) — called after
        each completed future (from main thread or background thread).
        """
        import concurrent.futures

        # Ensure DB is initialized (WAL + schema) before dispatching workers
        db_utils.init_db(self.db_path)

        to_process = self.filter_unprocessed(demo_paths, player_steamid, force=force_reprocess)
        if not to_process:
            logger.info("All demos already processed — nothing to do.")
            return []

        assigned = self.pre_assign_match_ids(to_process)
        total = len(assigned)
        results: List[Dict] = []
        errors: List[str] = []

        worker_args = [
            (str(p), player_steamid, mid, self.db_path, tickrate)
            for mid, p in assigned
        ]

        logger.info(f"Starting batch: {total} demos, {self.n_workers} workers")

        with concurrent.futures.ProcessPoolExecutor(
            max_workers=self.n_workers
        ) as pool:
            futures = {
                pool.submit(analyze_demo_worker, arg): arg[0]
                for arg in worker_args
            }
            done_count = 0
            for future in concurrent.futures.as_completed(futures):
                demo_path_str = futures[future]
                try:
                    result = future.result()
                except Exception as exc:
                    result = {
                        "status": "error",
                        "demo": Path(demo_path_str).stem,
                        "error": str(exc),
                        "traceback": _traceback.format_exc(),
                    }

                results.append(result)
                done_count += 1

                if result["status"] == "error":
                    err_msg = (
                        f"FAILED: {result.get('demo', demo_path_str)} — "
                        f"{result.get('error', 'unknown')}\n"
                        f"{result.get('traceback', '')}"
                    )
                    logger.error(err_msg)
                    errors.append(err_msg)
                else:
                    logger.info(
                        f"[{done_count}/{total}] OK: {result['demo']} — "
                        f"{result['engagements']} engagements, "
                        f"{result['attempts']} attempts"
                    )

                if progress_callback is not None:
                    try:
                        progress_callback(
                            done_count, total, result.get("demo", "")
                        )
                    except Exception:
                        pass  # Never let UI callback crash the runner

        if errors:
            logger.warning(f"Batch complete: {len(errors)}/{total} demos failed. See {BATCH_ERRORS_LOG}.")
        else:
            logger.info(f"Batch complete: {total}/{total} demos processed successfully.")

        return results
```

Для тестов в `tests/test_batch_runner.py`: создать файл с тестами для picklability, scan_demos,
filter_unprocessed, pre_assign_match_ids, и worker error return.

Тесты НЕ запускают реальный ProcessPoolExecutor с демо — используют mock/tmp fixtures.
Пример теста для picklability:
```python
import pickle
from batch_runner import analyze_demo_worker

def test_worker_is_picklable():
    data = pickle.dumps(analyze_demo_worker)
    assert data is not None
```

Пример теста для worker error return:
```python
from batch_runner import analyze_demo_worker

def test_worker_returns_error_dict_for_bad_path():
    result = analyze_demo_worker(("/nonexistent/demo.dem", 12345, 99, ":memory:", 64))
    assert result["status"] == "error"
    assert "traceback" in result
    assert result["match_id"] == 99
```

Пример теста для scan_demos:
```python
def test_scan_demos_returns_dem_files(tmp_path):
    (tmp_path / "a.dem").touch()
    (tmp_path / "b.dem").touch()
    (tmp_path / "notes.txt").touch()
    runner = BatchRunner(db_path=":memory:")
    result = runner.scan_demos(str(tmp_path))
    assert len(result) == 2
    assert all(p.suffix == ".dem" for p in result)
```
  </action>

  <verify>
    <automated>python -m pytest tests/test_batch_runner.py -x -q --override-ini="addopts=--strict-markers" 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "def analyze_demo_worker" batch_runner.py` returns 1
    - `grep -c "class BatchRunner" batch_runner.py` returns 1
    - `grep -c "def scan_demos" batch_runner.py` returns 1
    - `grep -c "def pre_assign_match_ids" batch_runner.py` returns 1
    - `grep -c "def filter_unprocessed" batch_runner.py` returns 1
    - `python -c "import pickle; from batch_runner import analyze_demo_worker; pickle.dumps(analyze_demo_worker); print('picklable OK')"` exits 0
    - `python -m pytest tests/test_batch_runner.py -x -q` exits 0
    - analyze_demo_worker с несуществующим путём возвращает status="error" (не бросает исключение)
  </acceptance_criteria>

  <done>
    batch_runner.py создан. analyze_demo_worker — top-level, picklable, возвращает dict.
    BatchRunner.run() pre-assigns match_ids, фильтрует processed, запускает ProcessPoolExecutor.
    Все tests/test_batch_runner.py зелёные.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → worker args | demo_path из for_analysis/ scan, player_steamid из Streamlit input |
| worker process → SQLite | каждый worker пишет в общий analytics.db через WAL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | analyze_demo_worker — demo_path arg | mitigate | Path(demo_path).stem для demo_name — нет shell injection; только SQLite parameterized queries |
| T-07-05 | Denial of Service | ProcessPoolExecutor 8 workers × 300MB RAM | accept | Пользователь сам выбирает n_workers через slider; документировано в D-05 |
| T-07-06 | Tampering | player_steamid из Streamlit input | mitigate | int() cast с ValueError в app.py до передачи в BatchRunner (существующий паттерн) |
</threat_model>

<verification>
```bash
# Picklability check
python -c "
import pickle
from batch_runner import analyze_demo_worker
data = pickle.dumps(analyze_demo_worker)
print(f'analyze_demo_worker picklable: {len(data)} bytes OK')
"

# Worker error return check
python -c "
from batch_runner import analyze_demo_worker
r = analyze_demo_worker(('/nonexistent.dem', 123456, 99, 'test.db', 64))
assert r['status'] == 'error', f'Expected error, got: {r}'
assert 'traceback' in r
print('Worker error handling OK')
"

# Unit tests
python -m pytest tests/test_batch_runner.py -x -q --override-ini="addopts=--strict-markers"
```
</verification>

<success_criteria>
1. `batch_runner.py` существует, `analyze_demo_worker` — top-level функция
2. `pickle.dumps(analyze_demo_worker)` не бросает исключение
3. Worker с несуществующим демо возвращает `{"status": "error", "traceback": "..."}`, не бросает
4. `BatchRunner.scan_demos()` возвращает только `.dem` файлы, отсортированные
5. `BatchRunner.filter_unprocessed()` пропускает demos в `processed_matches`
6. `BatchRunner.pre_assign_match_ids()` возвращает последовательные IDs начиная от `get_next_match_id()`
7. `python -m pytest tests/test_batch_runner.py -x` зелёный
</success_criteria>

<output>
После завершения создать `.planning/phases/07-parallel-batch-runner-data-accumulation/07-02-SUMMARY.md`
</output>
