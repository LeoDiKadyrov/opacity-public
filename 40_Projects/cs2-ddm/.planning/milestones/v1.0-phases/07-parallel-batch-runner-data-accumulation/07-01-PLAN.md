---
phase: 07-parallel-batch-runner-data-accumulation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db_utils.py
  - config.py
autonomous: true
requirements:
  - "Phase 7 SC2: идемпотентность (processed_matches table)"
  - "Phase 7 SC3: WAL mode для параллельных writers"
  - "Phase 7 D-06: match_id auto-increment"
  - "Phase 7 D-07: demo_name column migration"
  - "Phase 7 D-10: processed_matches table"

must_haves:
  truths:
    - "analytics.db использует WAL journal mode после вызова init_db()"
    - "Таблица processed_matches существует с PRIMARY KEY (demo_filename, player_steamid)"
    - "Таблица duel_attempts существует в analytics.db (была отсутствующей)"
    - "Колонки demo_name и player_steamid добавлены к таблице engagements (idempotent ALTER TABLE)"
    - "is_processed() возвращает False до, True после mark_processed()"
    - "get_next_match_id() возвращает MAX+1 по обеим таблицам"
    - "DEFAULT_BATCH_WORKERS = 8 определена в config.py"
    - "for_analysis/ директория существует на диске"
  artifacts:
    - path: "db_utils.py"
      provides: "init_db, _migrate_schema, is_processed, mark_processed, get_next_match_id"
      contains: "def init_db"
    - path: "config.py"
      provides: "DEFAULT_BATCH_WORKERS constant"
      contains: "DEFAULT_BATCH_WORKERS"
  key_links:
    - from: "db_utils.init_db()"
      to: "analytics.db"
      via: "PRAGMA journal_mode=WAL"
      pattern: "journal_mode=WAL"
    - from: "db_utils.mark_processed()"
      to: "processed_matches table"
      via: "INSERT OR REPLACE"
      pattern: "INSERT OR REPLACE INTO processed_matches"
---

<objective>
Создать фундамент Phase 7: WAL mode в analytics.db, schema migration для demo_name/player_steamid,
таблицы processed_matches и duel_attempts, хелперы идемпотентности, константу DEFAULT_BATCH_WORKERS,
и директорию for_analysis/.

Purpose: Все последующие планы зависят от этих изменений — без WAL параллельные workers получат
`database is locked`; без processed_matches нет идемпотентности; без duel_attempts таблицы workers
упадут при первой записи duel attempts.

Output: Расширенный db_utils.py (5 новых функций), config.py с DEFAULT_BATCH_WORKERS, for_analysis/ на диске.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-parallel-batch-runner-data-accumulation/07-CONTEXT.md
@.planning/phases/07-parallel-batch-runner-data-accumulation/07-RESEARCH.md

<interfaces>
<!-- Существующий db_utils.py — нужно расширить, не заменить -->
<!-- db_utils.py lines 1-57 -->
```python
import sqlite3
from contextlib import closing
from typing import Union
import pandas as pd

_ALLOWED_TABLES = {"engagements", "duel_attempts"}

def save_to_db(df: pd.DataFrame, db_path: str, table: str, match_id: Union[int, str]) -> None:
    """Idempotent: DELETE matching match_id, then INSERT. Uses 'with conn:' implicit transaction."""

def _table_exists(conn: sqlite3.Connection, table: str) -> bool:
    """SELECT name FROM sqlite_master WHERE type='table' AND name=?"""
```

<!-- config.py — группировка констант через comment banners -->
<!-- Существующий паттерн:
# ─────────────────────────────────────────────────────────────────────────────
# Group name
# ─────────────────────────────────────────────────────────────────────────────
CONSTANT_NAME: type = value
-->

<!-- analytics.db текущее состояние (VERIFIED):
- journal_mode = delete  ← нужно мигрировать на WAL
- Таблица engagements: существует (3 строки), НЕТ колонок demo_name, player_steamid
- Таблица duel_attempts: НЕ СУЩЕСТВУЕТ
- Таблица processed_matches: НЕ СУЩЕСТВУЕТ
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Расширить db_utils.py — init_db, schema migration, idempotency helpers</name>
  <files>db_utils.py, tests/test_db_utils.py</files>

  <read_first>
    - db_utils.py (полностью — 57 строк, уже в контексте выше)
    - tests/test_db_utils.py (полностью — проверить существующие тесты перед добавлением новых)
  </read_first>

  <behavior>
    - Test: init_db() на пустой temp БД → journal_mode возвращает 'wal'
    - Test: init_db() идемпотентен — вызов дважды не падает
    - Test: после init_db() таблица processed_matches существует с колонками (demo_filename, player_steamid, match_id, processed_at)
    - Test: после init_db() таблица duel_attempts существует
    - Test: _migrate_schema() на engagements без demo_name → добавляет колонку demo_name TEXT DEFAULT NULL
    - Test: _migrate_schema() на engagements без player_steamid → добавляет player_steamid INTEGER DEFAULT NULL
    - Test: _migrate_schema() идемпотентна — не падает если колонки уже есть
    - Test: is_processed() возвращает False для несуществующей записи
    - Test: is_processed() возвращает True после mark_processed()
    - Test: mark_processed() с одинаковыми (demo_filename, player_steamid) дважды → INSERT OR REPLACE, не IntegrityError
    - Test: get_next_match_id() на пустой БД возвращает 1
    - Test: get_next_match_id() возвращает MAX(engagements.match_id) + 1 когда таблица непустая
    - Test: get_next_match_id() берёт максимум по ОБЕИМ таблицам (engagements и duel_attempts)
  </behavior>

  <action>
Добавить в db_utils.py следующие функции (после существующих функций, сохранив весь существующий код):

1. **`init_db(db_path: str) -> None`** — вызывать при старте app.py и в каждом worker.
   Устанавливает WAL + запускает _migrate_schema().

```python
def init_db(db_path: str) -> None:
    """Initialize analytics.db: WAL mode + idempotent schema migration.

    Safe to call multiple times. WAL mode persists to DB file — subsequent
    connections inherit it automatically.
    """
    with closing(sqlite3.connect(db_path)) as conn:
        conn.execute("PRAGMA journal_mode=WAL")
        conn.execute("PRAGMA busy_timeout=10000")
        with conn:
            _migrate_schema(conn)
```

2. **`_migrate_schema(conn: sqlite3.Connection) -> None`** — идемпотентная миграция.

```python
def _migrate_schema(conn: sqlite3.Connection) -> None:
    """Idempotent schema migration. Adds columns/tables if missing."""
    existing_tables = {r[0] for r in conn.execute(
        "SELECT name FROM sqlite_master WHERE type='table'"
    ).fetchall()}

    # engagements: add demo_name and player_steamid if missing
    if "engagements" in existing_tables:
        cols = {c[1] for c in conn.execute("PRAGMA table_info(engagements)").fetchall()}
        if "demo_name" not in cols:
            conn.execute("ALTER TABLE engagements ADD COLUMN demo_name TEXT DEFAULT NULL")
        if "player_steamid" not in cols:
            conn.execute("ALTER TABLE engagements ADD COLUMN player_steamid INTEGER DEFAULT NULL")

    # duel_attempts: create if missing (was absent from analytics.db after Phase 6)
    conn.execute("""
        CREATE TABLE IF NOT EXISTS duel_attempts (
            match_id TEXT,
            map_name TEXT,
            demo_name TEXT,
            player_steamid INTEGER,
            t0_tick INTEGER,
            enemy_steamid INTEGER,
            was_killed INTEGER,
            bullets_fired INTEGER,
            bullets_hit INTEGER,
            engagement_type TEXT,
            player_velocity_ups REAL,
            crosshair_angle_deg REAL,
            hurt_victims_in_window TEXT,
            fires_in_cluster INTEGER
        )
    """)

    # processed_matches: idempotency tracker for batch runner
    conn.execute("""
        CREATE TABLE IF NOT EXISTS processed_matches (
            demo_filename TEXT NOT NULL,
            player_steamid INTEGER NOT NULL,
            match_id INTEGER NOT NULL,
            processed_at TEXT NOT NULL,
            PRIMARY KEY (demo_filename, player_steamid)
        )
    """)
```

3. **`is_processed(db_path: str, demo_filename: str, player_steamid: int) -> bool`**

```python
def is_processed(db_path: str, demo_filename: str, player_steamid: int) -> bool:
    """Return True if (demo_filename, player_steamid) exists in processed_matches."""
    with closing(sqlite3.connect(db_path)) as conn:
        row = conn.execute(
            "SELECT 1 FROM processed_matches WHERE demo_filename=? AND player_steamid=?",
            (demo_filename, player_steamid),
        ).fetchone()
        return row is not None
```

4. **`mark_processed(db_path: str, demo_filename: str, player_steamid: int, match_id: int) -> None`**

```python
def mark_processed(
    db_path: str, demo_filename: str, player_steamid: int, match_id: int
) -> None:
    """Record that (demo_filename, player_steamid) has been processed."""
    from datetime import datetime, timezone
    with closing(sqlite3.connect(db_path)) as conn:
        with conn:
            conn.execute(
                """INSERT OR REPLACE INTO processed_matches
                   (demo_filename, player_steamid, match_id, processed_at)
                   VALUES (?, ?, ?, ?)""",
                (demo_filename, player_steamid, match_id,
                 datetime.now(timezone.utc).isoformat()),
            )
```

5. **`get_next_match_id(db_path: str) -> int`** — pre-assignment в main thread.

```python
def get_next_match_id(db_path: str) -> int:
    """Return MAX(match_id) + 1 across engagements and duel_attempts tables.

    Returns 1 if both tables are empty or absent.
    """
    with closing(sqlite3.connect(db_path)) as conn:
        r1 = conn.execute(
            "SELECT MAX(CAST(match_id AS INTEGER)) FROM engagements"
        ).fetchone()[0] if _table_exists(conn, "engagements") else None
        r2 = conn.execute(
            "SELECT MAX(CAST(match_id AS INTEGER)) FROM duel_attempts"
        ).fetchone()[0] if _table_exists(conn, "duel_attempts") else None
        current_max = max(r1 or 0, r2 or 0)
        return current_max + 1
```

Импорты в db_utils.py: добавить `from typing import Union` уже есть. Новые импорты не нужны — `datetime` импортируется inline в mark_processed.

Для тестов в tests/test_db_utils.py: использовать `tmp_path` fixture (pytest built-in), создавать temp DB.
Паттерн:
```python
def test_init_db_wal_mode(tmp_path):
    db = str(tmp_path / "test.db")
    init_db(db)
    with closing(sqlite3.connect(db)) as conn:
        mode = conn.execute("PRAGMA journal_mode").fetchone()[0]
    assert mode == "wal"
```
  </action>

  <verify>
    <automated>python -m pytest tests/test_db_utils.py -x -q --override-ini="addopts=--strict-markers" 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "def init_db" db_utils.py` returns 1
    - `grep -c "def _migrate_schema" db_utils.py` returns 1
    - `grep -c "def is_processed" db_utils.py` returns 1
    - `grep -c "def mark_processed" db_utils.py` returns 1
    - `grep -c "def get_next_match_id" db_utils.py` returns 1
    - `grep -c "PRAGMA journal_mode=WAL" db_utils.py` returns 1
    - `grep -c "processed_matches" db_utils.py` >= 3
    - `python -m pytest tests/test_db_utils.py -x -q` exits 0
    - All 13 behavior tests pass (see tdd behavior block above)
  </acceptance_criteria>

  <done>
    db_utils.py содержит 5 новых функций. Все tests/test_db_utils.py (существующие + новые) зелёные.
    analytics.db после init_db() → journal_mode = wal, таблицы processed_matches и duel_attempts существуют.
  </done>
</task>

<task type="auto">
  <name>Task 2: Добавить DEFAULT_BATCH_WORKERS в config.py и создать for_analysis/</name>
  <files>config.py, for_analysis/.gitkeep</files>

  <read_first>
    - config.py (строки 130-157 — конец файла, для правильного места вставки и сохранения стиля)
  </read_first>

  <action>
1. **config.py**: добавить секцию в конец файла после существующего блока Logging:

```python
# ─────────────────────────────────────────────────────────────────────────────
# Batch runner
# ─────────────────────────────────────────────────────────────────────────────

# Default number of parallel worker processes for batch demo analysis.
# Matches i7-11800H physical core count (8 cores / 16 logical).
# Each worker loads awpy + demoparser2 + numpy (~300 MB RAM per process).
DEFAULT_BATCH_WORKERS: int = 8

# Directory scanned by the batch runner for .dem input files.
BATCH_INPUT_DIR: str = "for_analysis"

# Log file for batch worker errors (tracebacks from failed demos).
BATCH_ERRORS_LOG: str = "batch_errors.log"
```

2. **for_analysis/.gitkeep**: создать пустой файл чтобы директория попала в git и существовала на диске.
   Также убедиться что for_analysis/ директория существует: `from pathlib import Path; Path("for_analysis").mkdir(exist_ok=True)`.
   Создать файл `for_analysis/.gitkeep` с пустым содержимым.

Не изменять никакой существующий код в config.py — только append в конец.
  </action>

  <verify>
    <automated>python -c "from config import DEFAULT_BATCH_WORKERS, BATCH_INPUT_DIR, BATCH_ERRORS_LOG; print(DEFAULT_BATCH_WORKERS, BATCH_INPUT_DIR, BATCH_ERRORS_LOG); assert DEFAULT_BATCH_WORKERS == 8"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "DEFAULT_BATCH_WORKERS" config.py` returns 1
    - `grep -c "BATCH_INPUT_DIR" config.py` returns 1
    - `grep -c "BATCH_ERRORS_LOG" config.py` returns 1
    - `python -c "from config import DEFAULT_BATCH_WORKERS; assert DEFAULT_BATCH_WORKERS == 8"` exits 0
    - `for_analysis/.gitkeep` существует на диске
    - `python -m pytest tests/test_config.py -x -q` exits 0 (регрессия — существующие тесты)
  </acceptance_criteria>

  <done>
    config.py содержит DEFAULT_BATCH_WORKERS = 8, BATCH_INPUT_DIR, BATCH_ERRORS_LOG.
    Директория for_analysis/ существует. Все существующие тесты зелёные.
  </done>
</task>


<task type="auto">
  <name>Task 3: Добавить force_reprocess_demo() в db_utils.py</name>
  <files>db_utils.py</files>

  <read_first>
    - db_utils.py (полностью — убедиться в стиле existing functions перед добавлением)
    - .planning/phases/07-parallel-batch-runner-data-accumulation/07-CONTEXT.md (D-12 locked decision)
  </read_first>

  <action>
Добавить в db_utils.py после mark_processed():

```python
def force_reprocess_demo(db_path: str, demo_filename: str, player_steamid: int) -> int:
    """Delete all rows for a previously processed demo so it can be re-run.

    D-12: force mode deletes processed_matches record and all rows for that match_id.
    Returns the old match_id (0 if demo was not processed).
    """
    with closing(sqlite3.connect(db_path)) as conn:
        with conn:
            row = conn.execute(
                "SELECT match_id FROM processed_matches WHERE demo_filename=? AND player_steamid=?",
                (demo_filename, player_steamid),
            ).fetchone()
            if row is None:
                return 0
            old_match_id = row[0]
            conn.execute("DELETE FROM engagements WHERE match_id=?", (old_match_id,))
            conn.execute("DELETE FROM duel_attempts WHERE match_id=?", (old_match_id,))
            conn.execute(
                "DELETE FROM processed_matches WHERE demo_filename=? AND player_steamid=?",
                (demo_filename, player_steamid),
            )
            return old_match_id
```
  </action>

  <verify>
    <automated>python -c "from db_utils import force_reprocess_demo; print('force_reprocess_demo importable OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "def force_reprocess_demo" db_utils.py` returns 1
    - `grep -c "DELETE FROM engagements WHERE match_id" db_utils.py` returns 1
    - `grep -c "DELETE FROM duel_attempts WHERE match_id" db_utils.py` returns 1
    - `grep -c "DELETE FROM processed_matches WHERE demo_filename" db_utils.py` returns 1
    - Function returns 0 when demo not in processed_matches
    - `python -m pytest tests/test_db_utils.py -x -q` exits 0
  </acceptance_criteria>

  <done>
    db_utils.py содержит force_reprocess_demo(). D-12 locked decision полностью реализован.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → db_utils | demo_filename приходит из for_analysis/ scan; используется как параметр SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | db_utils.mark_processed() — demo_filename param | mitigate | Parameterized query `(?, ?)` — уже реализовано в паттерне |
| T-07-02 | Tampering | _migrate_schema() f-string table names | accept | Table names hardcoded как строковые литералы в функции, не из user input |
| T-07-03 | Tampering | ALTER TABLE ADD COLUMN — column names hardcoded | accept | Hardcoded column names ("demo_name", "player_steamid") — нет user input |
</threat_model>

<verification>
```bash
# WAL mode активирован
python -c "
import sqlite3, db_utils
from contextlib import closing
db_utils.init_db('analytics.db')
with closing(sqlite3.connect('analytics.db')) as c:
    mode = c.execute('PRAGMA journal_mode').fetchone()[0]
    tables = [r[0] for r in c.execute(\"SELECT name FROM sqlite_master WHERE type='table'\").fetchall()]
    cols = [c[1] for c in c.execute('PRAGMA table_info(engagements)').fetchall()]
print('journal_mode:', mode)
print('tables:', tables)
print('engagements cols:', cols)
assert mode == 'wal'
assert 'processed_matches' in tables
assert 'duel_attempts' in tables
assert 'demo_name' in cols
assert 'player_steamid' in cols
print('ALL CHECKS PASSED')
"

# Тесты
python -m pytest tests/test_db_utils.py tests/test_config.py -x -q --override-ini="addopts=--strict-markers"
```
</verification>

<success_criteria>
1. `PRAGMA journal_mode` на analytics.db возвращает `wal`
2. Таблицы `processed_matches`, `duel_attempts` существуют в analytics.db
3. Колонки `demo_name` и `player_steamid` есть в engagements (NULL для 3 старых строк)
4. `is_processed(db, "test.dem", 123)` → False; после `mark_processed(db, "test.dem", 123, 1)` → True
5. `get_next_match_id(db)` → 1 на пустой БД
6. `DEFAULT_BATCH_WORKERS == 8` импортируется из config
7. `for_analysis/` директория существует
8. `python -m pytest tests/test_db_utils.py -x` зелёный (все тесты, включая новые)
</success_criteria>

<output>
После завершения создать `.planning/phases/07-parallel-batch-runner-data-accumulation/07-01-SUMMARY.md`
</output>
